---
issue: 75
status: proposed-product-specification
workflow_contract: workflow-contract-v1
verification_contract: workflow-verification-contract-v1
science_contract: analysis-contract-v1
validation_contract: validation-contract-v1
adr: ADR-0002
---

# 改造焦斑分析便携客户端的中文自动化工作区

> 本文冻结产品工作流，不改写 `analysis-contract-v1` 或 `validation-contract-v1` 的科学语义。实现和验收应以 `workflow-contract-v1` 与 `workflow-verification-contract-v1` 为准；在实现票和便携发布验收完成前，不得把本规格当作已验证产品证据。

## Problem Statement

当前 Spot Analysis 便携客户端虽然已经能够调用分析 worker、显示部分结果并导出报告，但实际 `.exe` 无法作为顺畅、可信的日常焦斑分析工具使用：

- 主界面和动态提示大量使用英文，不符合中文用户的操作习惯；部分业务逻辑还依赖英文显示文本，无法通过简单替换文案安全完成中文化。
- 当前客户端只接受灰度 PNG，拒绝主要真实输入目录中的等通道 RGB PNG；这些图片虽然以 RGB 容器保存，但每个像素均满足 `R=G=B`，可以无损解释为灰度强度。
- 分析区域（ROI）初始值为不可用的 `1×1`，只能手填坐标，缺少自动主焦斑定位、图像叠加、拖动、缩放和重定位能力。
- 背景区域在界面上被描述为可选，但分析核心实际上要求背景支持，导致默认配置看似可用却无法完成分析。
- 当前工作流要求用户先填写大量字段、确认配置、再手动运行；图片载入后不会自动形成可检查的初始结果。用户需要的流程是自动给出可用预览，仅在最后复核分析区域并生成正式结果时进行一次人工操作。
- 当前空间标定默认值没有可信来源，可能把未标定图片错误显示为具有物理尺寸精度的测量。
- 生产 WPF 界面只部分实现已经接受的 Prototype A；图层、显示范围、图像操作、结果层级、质量解释及 X/Y 剖面等关键能力尚未形成完整工作区。
- 当前客户端只支持单图状态，不能一次选择多张图片、逐张复核、保存各图状态或批量导出正式结果。
- 当前结果与报告门控不足以清楚区分预览结果、正式结果、计算成功、测量有效性和需要重新计算。
- 当前发布验收没有覆盖主要真实 PNG、中文路径、多图工作流、Prototype 对齐、DPI、交互性能和干净目标环境。

这些问题彼此关联：单独翻译界面或放宽 PNG 过滤都不能形成可用的端到端产品。本改造必须作为同一版本的整体验收门槛交付。

## Solution

将现有 WPF 便携客户端改造为以 Prototype A 为信息架构和视觉基线的简体中文自动化焦斑分析工作区，同时保留已接受的 WPF 外壳、独立 Python 分析核心、版本化 NDJSON worker interface 和不可变分析记录边界。

用户可以一次选择一张或多张受支持 PNG。当前图片立即显示，系统自动解码、选择背景样本、检测主焦斑、生成自适应分析区域并计算预览结果。用户可以直接在图像上移动、缩放或重定位 ROI，预览随配置更新。正常流程只保留一个最终人工操作：“确认分析区域并生成正式结果”。

预览结果和正式结果成为不同的领域状态。预览结果支持检查和调整，不可作为报告来源；正式结果记录用户最终确认的 ROI 和相对强度测量语义，但“正式”不等于“有效”。质量不满足的正式结果仍可形成可追溯分析记录和报告，报告必须突出显示无效、需复核或不可用状态及原因，不得隐藏失败证据。

输入范围扩展为 8/16 位灰度 PNG，以及所有像素严格满足 `R=G=B` 的 8 位 RGB PNG。等通道 RGB 只无损提取单一通道，不执行加权灰度、Gamma、颜色管理或降位转换。WPF、Python 输入适配器及 worker 必须共享一致的输入语义。

未提供可信空间标定时，系统仍以像素单位完成预览和正式分析；物理单位指标显示为未标定或不可用，不使用演示值或占位值伪造物理尺寸。默认预处理、自动 ROI、自动背景、模型和质量参数来自具名、版本化的分析档案。

完成后的工作区采用左侧工作流配置、中央图像与显示工具、右侧结果与报告、下方解释曲线的紧凑日间布局。主界面、报告和用户文档使用简体中文，领域缩写、单位和机器字段按约定保留。显示图层、伪彩、拉伸、缩放和平移只改变显示图像，绝不改变测量数据或分析记录。

本规格通过一个高层工作区 presentation seam 验证绝大多数外部行为，并复用现有分析核心、worker、显示投影和报告 seam。最终验收必须针对 fresh extraction 的实际便携发布包和主要真实 PNG，而不只针对开发构建或源代码。

## User Stories

1. 作为中文焦斑分析用户，我想让应用标题、菜单、按钮、标签和状态使用简体中文，以便无需理解普通英文界面。
2. 作为用户，我想让错误、警告、帮助和操作提示使用简体中文，以便知道发生了什么以及如何处理。
3. 作为用户，我想保留 ROI、FWHM、D4σ、EE、PNG、PDF 等稳定缩写，以便与领域资料和实验记录一致。
4. 作为用户，我想让缩写首次出现时附带中文名称，以便理解其含义。
5. 作为用户，我想让路径、文件名、哈希和机器协议值保持原样，以便可追溯和脚本兼容。
6. 作为用户，我想让报告中的标题、指标、质量说明和结论使用中文，以便报告可以直接审阅和交流。
7. 作为诊断人员，我想在中文质量说明后看到原始原因码，以便检索和排障。
8. 作为用户，我想打开主要采集目录中的 `.png` 或 `.PNG` 文件，以便分析真实实验数据。
9. 作为用户，我想打开 8 位灰度 PNG，以便分析常见灰度采集图像。
10. 作为用户，我想打开 16 位灰度 PNG，以便保留高位深强度数据。
11. 作为用户，我想打开三个通道完全相同的 8 位 RGB PNG，以便分析由采集软件用 RGB 容器保存的灰度数据。
12. 作为用户，我想让等通道 RGB 被无损读取，而不是进行加权彩色转灰度，以便不改变测量强度。
13. 作为用户，我想在真正的彩色 RGB、RGBA 或调色板 PNG 不受支持时看到明确中文原因，以便不会遇到程序崩溃或模糊错误。
14. 作为用户，我想让输入摘要显示尺寸、位深、通道和等通道处理事实，以便理解程序如何解释图片。
15. 作为审计人员，我想让分析记录保存输入哈希、原始通道和解码方式，以便复现输入语义。
16. 作为用户，我想一次选择多张图片，以便连续处理同一批采集结果。
17. 作为用户，我想让当前图片优先显示和预处理，以便不用等待所有图片完成。
18. 作为用户，我想让其他图片在不影响当前交互的情况下低优先级准备预览，以便切换时更快。
19. 作为用户，我想在图片列表中看到未处理、预览、待复核、正式、警告、失败和已导出状态，以便掌握整体进度。
20. 作为用户，我想在切换图片后恢复该图片自己的 ROI、标定、结果和警告，以便不同图片状态不会串用。
21. 作为用户，我想让单张图片失败不影响其他图片，以便继续完成批次工作。
22. 作为用户，我想在打开图片后立即看到可操作的输入图像，以便确认选中了正确文件。
23. 作为用户，我想让系统自动执行默认预处理并产生预览结果，以便不需要逐步确认配置。
24. 作为用户，我想让正常流程不出现“确认图片”“应用参数”和“下一步”等中间门槛，以便减少重复操作。
25. 作为用户，我想在后台分析运行时继续查看状态或切换图片，以便应用不会显得卡死。
26. 作为用户，我想取消当前后台处理，以便快速改用其他图片或配置。
27. 作为用户，我想只在最后复核 ROI 后执行一次“确认分析区域并生成正式结果”，以便人工操作集中在真正需要判断的位置。
28. 作为用户，我想让最终操作同时确认当前 ROI 和按相对强度码值测量的声明，以便不需要额外确认框。
29. 作为用户，我想清楚区分预览结果和正式结果，以便不会误导出未经复核的自动结果。
30. 作为用户，我想让预览结果不能导出为测量报告，以便报告只来自最终确认配置。
31. 作为用户，我想让系统自动定位主焦斑，以便默认 ROI 能直接用于检查。
32. 作为用户，我想让主焦斑根据排除孤立坏点后的积分能量确定，以便单个热像素不会抢占 ROI。
33. 作为用户，我想在检测到多个能量接近的候选焦斑时看到非阻断警告，以便人工确认系统选择。
34. 作为用户，我想让系统仍自动选择能量较大的候选，以便多候选警告不会中断工作流。
35. 作为用户，我想在自动检测不可靠时获得居中的回退 ROI，以便流程仍有可用初始值。
36. 作为用户，我想让回退状态明确显示“未可靠定位主焦斑”，以便知道必须重点复核。
37. 作为用户，我想让 ROI 尺寸根据主焦斑范围自适应并保留安全边距，以便信号不被过紧截断。
38. 作为用户，我想让 ROI 完全位于图像范围内，以便计算域始终有效。
39. 作为用户，我想在 ROI 受图像边界限制或疑似截断信号时看到质量警告，以便理解结果限制。
40. 作为用户，我想在 ROI 内按住鼠标左键拖动整个矩形，以便快速移动分析区域。
41. 作为用户，我想单击图像中的其他位置重定位 ROI，以便快速检查另一候选焦斑。
42. 作为用户，我想拖动边和角控制点修改 ROI 宽高，以便调整计算域。
43. 作为用户，我想让 ROI 光标和高亮清楚区分移动与缩放，以便避免误操作。
44. 作为用户，我想让 ROI 不能拖出图像边界，以便保持有效配置。
45. 作为用户，我想用方向键小步移动 ROI，并用修饰键大步移动，以便精确定位。
46. 作为用户，我想通过数值字段精确编辑 ROI 坐标和尺寸，以便满足可重复设置需求。
47. 作为用户，我想让 ROI 修改后自动更新预览，以便无需点击“重新分析”。
48. 作为用户，我想让拖动过程中的覆盖层实时响应，并在计算适合时实时或松开后短延迟更新结果，以便交互流畅。
49. 作为用户，我想选择锁定当前 ROI 并应用到后续图片，以便处理同一机位的采集批次。
50. 作为用户，我想让未锁定时每张新图重新检测 ROI，以便不会静默继承不适用区域。
51. 作为用户，我想让系统自动选择 ROI 外的背景样本，以便正常流程不要求手填背景矩形。
52. 作为用户，我想让自动背景排除主焦斑、明显正信号、饱和点和坏点，以便背景估计不被污染。
53. 作为用户，我想让背景样本不与 ROI 重叠，以便背景和测量区域语义分离。
54. 作为用户，我想在背景支持不足时看到非阻断警告和真实指标状态，以便程序不会静默使用不可靠兜底。
55. 作为专业用户，我想显示自动背景样本叠加，以便检查背景来源。
56. 作为专业用户，我想在高级设置中手动指定背景区域，以便处理特殊图像。
57. 作为未标定图片的用户，我想仍以像素单位查看中心和尺寸结果，以便没有物理标定也能完成图像域分析。
58. 作为用户，我想让物理单位结果在未标定时明确显示“未标定”或不可用，以便不会误信占位数值。
59. 作为用户，我不想让系统自动使用 `1 mm/px` 或 Prototype 演示标定，以便避免虚假物理精度。
60. 作为用户，我想在高级设置中输入 x/y 空间标定、单位和来源，以便生成可信物理域结果。
61. 作为批次用户，我想显式把当前标定应用到全部已选图片，以便同一采集条件下减少重复输入。
62. 作为用户，我想让改变标定使受影响的正式结果进入“需要重新计算”，以便旧物理结果不会继续冒充当前结果。
63. 作为用户，我想让默认参数自动来自当前推荐分析档案，以便应用打开即可用。
64. 作为审计人员，我想让分析记录和报告包含档案及算法版本，以便知道实际使用的默认行为。
65. 作为专业用户，我想在折叠的高级设置中查看和修改允许调整的参数，以便处理特殊场景。
66. 作为用户，我想在修改参数后看到“已偏离推荐默认值”，以便知道当前结果不是标准默认分支。
67. 作为用户，我想一键恢复推荐默认值，以便快速回到标准配置。
68. 作为用户，我不想看到尚未验证或无法解释的参数，以便避免误用演示功能。
69. 作为用户，我想让非法参数就地显示中文反馈，而不是经过多个确认弹窗，以便快速修正。
70. 作为用户，我想在左侧看到输入、标定、预处理、模型和执行分析的清晰工作流，以便理解配置层级。
71. 作为用户，我想让中央区域充分展示图像，以便检查焦斑和 ROI，而不是查看小缩略图。
72. 作为用户，我想在右侧优先看到核心结果、质量状态和报告操作，以便快速完成复核。
73. 作为用户，我想同时看到 X/Y 实际与拟合剖面，以便判断焦斑形状和模型适用性。
74. 作为用户，我想看到累计能量曲线，以便理解 EE50 和 EE80。
75. 作为用户，我想切换输入图、校正图、模型图和残差图，以便检查分析证据。
76. 作为用户，我想在图层尚不可用时看到明确原因，以便不会把空白误认为渲染故障。
77. 作为用户，我想切换灰度和伪彩显示，以便观察不同强度细节。
78. 作为用户，我想使用自动分位数、全范围和手动范围，以便调整显示对比度。
79. 作为用户，我想缩放、平移并恢复图像视图，以便检查局部细节。
80. 作为用户，我想显示或隐藏 ROI、规范中心和背景样本等叠加，以便分别检查原图和分析依据。
81. 作为用户，我想让图例、说明和操作控件位于黑色图像框外，以便图像内容不被界面文字遮挡。
82. 作为测量用户，我想让伪彩、Gamma、拉伸、插值和显示平滑只影响显示图像，以便测量数据不被视觉设置改变。
83. 作为维护者，我想证明切换显示设置不会改变分析指纹、校正强度图或指标，以便保持显示与测量分离。
84. 作为用户，我想在结果卡片中看到焦斑中心、FWHM、D4σ、方向、EE 和集中度，以便快速读取核心指标。
85. 作为用户，我想让每项指标显示自己的有效、需复核、无效或不可用状态，以便不会用整体状态掩盖局部问题。
86. 作为用户，我想看到饱和、截断、多焦斑、背景和拟合质量的中文解释，以便知道结果为何降级。
87. 作为专业用户，我想展开查看拟合参数、原因码、输入哈希、档案版本和运行时间，以便排障和审计。
88. 作为用户，我想让计算成功与测量有效性分开显示，以便数值存在不会被误解为可信。
89. 作为用户，我想让拟合失败、饱和或质量不满足的已确认分析仍形成正式记录，以便失败证据可追溯。
90. 作为用户，我想让无法解释的指标显示空值或 N/A，而不是内部尝试数值，以便避免误用。
91. 作为用户，我想导出包含无效结果的报告，并让报告首页突出标明无效或部分不可用，以便完整保存分析事实。
92. 作为用户，我不想让报告静默忽略失败指标，以便审阅者能看到全部限制。
93. 作为用户，我想在修改 ROI、标定、预处理或模型后让旧正式结果显示“需要重新计算”，以便不会混用配置。
94. 作为审计人员，我想让旧正式结果保留而不被覆盖，以便回溯修改前后的分析。
95. 作为用户，我想让修改后自动产生新预览，并在再次最终确认时生成新的分析记录，以便工作流保持简单且可追溯。
96. 作为用户，我想默认导出 PNG 报告，以便与 Prototype A 和日常查看方式一致。
97. 作为用户，我想仍可选择 PDF，以便用于归档和打印。
98. 作为用户，我想默认附加时间后缀，以便不会覆盖已有报告。
99. 作为用户，我想让默认报告名包含中文“焦斑分析报告”和输入图像名，以便识别报告来源。
100. 作为用户，我想导出当前图片的正式报告，以便快速保存单次结果。
101. 作为批次用户，我想导出所有已有正式结果，以便减少重复操作。
102. 作为批次用户，我想让每张图片产生独立报告，以便不引入未经定义的跨图统计语义。
103. 作为批次用户，我想让无正式结果的图片被跳过并列入汇总，以便了解遗漏项。
104. 作为批次用户，我想让单张导出失败不终止其他报告，以便尽可能完成批量导出。
105. 作为用户，我想在 `1920×1080` 下获得符合 Prototype A 的主要工作区，以便充分利用常用显示器。
106. 作为用户，我想在 `1366×768` 下仍能访问全部主流程，以便较小屏幕不会阻断操作。
107. 作为高 DPI 用户，我想在 Windows 100%–200% 缩放下看到不截断、不重叠的中文文字，以便界面保持可用。
108. 作为用户，我想让小窗口使用合理滚动、折叠或分隔条，而不是隐藏操作，以便无需固定全屏。
109. 作为键盘用户，我想按符合视觉流程的 Tab 顺序操作，以便不依赖鼠标。
110. 作为低视力用户，我想让状态同时使用中文、图标和颜色，以便不只依赖颜色识别。
111. 作为用户，我想用快捷键打开图片、恢复视图和生成正式结果，以便提高常用操作效率。
112. 作为用户，我想在打开 `1600×1200` 图片后约 1 秒内看到可操作输入图，以便工作流即时响应。
113. 作为用户，我想让 ROI 和画布操作保持连续，以便完整分析不会阻塞界面线程。
114. 作为用户，我想在松开 ROI 后目标 2 秒内看到预览更新，并在更慢时看到进度，以便理解系统状态。
115. 作为用户，我想在中文、空格和较长路径中打开输入和保存报告，以便适配真实 Windows 文件环境。
116. 作为用户，我想直接运行便携客户端而无需 Python、.NET SDK、管理员权限或网络服务，以便在实验机器部署。
117. 作为测试人员，我想用真实三张 PNG 验证解码、自动预览、ROI 和正式结果，以便测试覆盖主要输入。
118. 作为测试人员，我想用多焦斑、孤立坏点、饱和、截断和检测失败场景验证候选与质量状态，以便覆盖关键边界。
119. 作为测试人员，我想在真实 WPF 窗口中验证鼠标、键盘、图层、DPI 和中文布局，以便 presentation 测试不会掩盖绑定错误。
120. 作为测试人员，我想从 fresh extraction 的发布包完成多图分析和批量导出，以便证明用户收到的 `.exe` 与开发构建一致。
121. 作为项目负责人，我想获得与 Prototype A 并排的截图、性能数据和人工验收记录，以便有证据决定版本是否完成。
122. 作为维护者，我想让中文显示值与稳定协议值分离，以便翻译不会改变状态颜色、标定校验或 worker 请求。
123. 作为维护者，我想复用唯一 Python 分析核心和 worker interface，以便 WPF 不产生第二套科学实现。
124. 作为维护者，我想让 UI 只管理工作区草稿、预览、正式记录和显示状态，以便领域边界保持清晰。
125. 作为维护者，我想让自动参数属于版本化档案，以便默认行为变化可追溯且旧记录可复现。
126. 作为维护者，我想用少量高层 seam 覆盖工作区行为，以便测试不依赖每个控件或事件处理器。
127. 作为文档读者，我想获得中文快速开始、支持格式、ROI 操作、标定和质量说明，以便独立完成分析。
128. 作为支持人员，我想让用户文档说明诊断导出和常见错误，以便远程排查问题。

## Implementation Decisions

### 产品交付和范围

- 五项整改构成同一版本的整体完成条件；允许拆分实现和评审，但任何一项未完成时总体验收不得关闭。
- Prototype A 是生产 WPF 工作区的信息架构、交互和视觉方向基线，不将 HTML、WebView2 或浏览器运行时引入发布客户端。
- 生产客户端继续采用原生 WPF 外壳、独立 Python worker 和版本化 NDJSON 通信；科学计算不得在 C# 中重写。
- 本轮只实现焦斑分析工作区，不显示不可用的独立绘图模式，也不提供无行为占位控件。
- Prototype 中缺少冻结科学规格的 Gamma 测量、超高斯等演示功能不进入本规格。

### 领域状态和工作流

- “分析区域（ROI）”是唯一规范区域术语；“ROT”“裁剪框”和未说明用途的“窗口”不作为界面或规格用语。
- “主焦斑”是排除孤立坏点后积分能量最大的连续焦斑结构；多个接近候选必须产生可见诊断。
- 工作区明确区分预览结果和正式结果。预览结果来自自动候选配置，不可进入测量报告；正式结果来自最终人工确认，可以进入报告门控。
- “正式”只表示用户确认了 ROI 和测量语义，不表示测量有效。流程状态、指标有效性和需要重新计算状态分别建模。
- 图片载入后自动进入预览流程。正常路径不要求逐步确认输入、预处理、背景或模型。
- 最终只有一个主操作，用于确认当前 ROI 和相对强度码值测量声明，并生成正式分析记录。
- 输入、ROI、标定、预处理档案或模型改变后，已有正式结果保留但标记为需要重新计算；当前配置自动生成新预览。
- 重新确认生成新的不可变分析记录，不覆盖旧记录。
- 可恢复的定位低置信度、背景不足或测量质量问题不弹出阻断确认；使用中文状态和原因继续展示预览。无法解码、参数域无效或完全无法产生结构化结果才阻断对应操作。
- 计算或质量失败可以形成正式记录。报告如实显示无效和不可用指标，不将“正式结果”错误等同于“有效结果”。

### 输入契约

- 支持原生 8 位灰度 PNG、原生 16 位灰度 PNG，以及所有像素严格满足 `R=G=B` 的 8 位 RGB PNG。
- 等通道 RGB 通过无损提取一个通道进入测量数据，保留原始通道数和等通道事实。
- 真正彩色 RGB、RGBA、调色板及其他未验证编码不静默转换，返回结构化中文错误。
- 测量输入不执行 Gamma、sRGB、ICC、加权灰度、插值或位深缩减。
- WPF 用户入口、Python 输入适配器和 worker 使用同一个输入策略及一致的成功/拒绝语义。
- 输入记录保留原始资产身份、哈希、尺寸、位深、通道、解码类型、编码元数据及其缺失状态。
- 主要真实目录中的三张等通道 RGB PNG 是发布契约测试样本，而不是可选人工样例。

### 自动 ROI 和主焦斑

- 自动检测以排除孤立坏点后的积分能量为主，不直接把全局最大单像素或图像中心当作主焦斑。
- 多候选接近程度、坏点排除、主焦斑分割、ROI 安全边距、最小尺寸和质量阈值由版本化分析档案定义。
- 多候选时自动提出积分能量最大的候选，同时记录候选数、能量比和选择原因，并显示非阻断复核警告。
- 定位失败时提出位于图像中心的有效矩形回退区域，并明确标注回退原因。
- 自动 ROI 包含主焦斑信号范围及安全边距，不使用 Prototype 演示尺寸作为固定默认。
- 自动候选显示后不为改善结果而后台静默改变；只有明确的新输入、配置变化或用户操作才改变工作区候选。
- 正式分析的拟合、D4σ、EE 和集中度继续使用同一最终确认 ROI。
- ROI 接触图像边界、疑似截断信号或不包含规范中心时产生结构化质量反馈；不得用静默扩域掩盖问题。
- ROI 保持为单个、轴对齐、完全位于图像范围内的矩形，不引入旋转、多区域或多边形分析域。
- 图像覆盖层、数值字段、鼠标操作和键盘操作共享同一 ROI 草稿状态，不能分别维护互相矛盾的坐标副本。

### 自动背景

- 缺少匹配背景帧时，系统自动提出 ROI 外背景保护样本，不再要求普通用户填写必需背景坐标。
- 自动背景排除坏点、饱和点、主焦斑候选和明显正信号，并满足背景模型要求的空间覆盖。
- 自动背景不与正式 ROI 重叠，背景来源、支持量、排除量、残差和回退状态进入分析记录。
- 支持不足时不静默使用全图中值；依赖背景的指标根据质量档案降级或无效，并向用户显示非阻断说明。
- 自动背景样本可以作为可选叠加查看，专业用户可以在高级设置中手动指定背景区域。
- 手动背景修改属于分析配置变化，会更新预览并使旧正式结果过期。

### 空间标定和单位

- 没有可信空间标定时允许完成像素域分析。像素域结果使用 `px`，物理域指标明确为未标定或不可用。
- 不使用当前 UI 占位值或 Prototype 演示值作为默认有效物理标定。
- 用户可以在高级设置中提供 x/y 长度每像素、单位、来源和状态。
- 标定默认只应用于当前图片；工作区提供显式“应用到本批次全部图片”操作。
- 批量应用标定不静默改变已有正式结果；受影响记录进入需要重新计算状态。
- 报告始终说明标定来源和状态，不能把 provisional 或用户声明值表示为校准证书。

### 默认档案和高级设置

- 标准预处理、坏点策略、自动背景、主焦斑检测、ROI 生成、模型、优化器和质量阈值由具名版本化档案提供。
- UI 不散布第二套业务默认值；请求快照记录所有实际参数和档案/算法版本。
- 档案语义变化通过新版本表达，不静默改变旧版本。
- 普通流程自动选择当前推荐档案，不要求用户逐项确认。
- 高级设置默认折叠，仅展示有规格、可解释且允许调整的参数。
- 修改参数时显示已偏离推荐档案，并提供恢复推荐默认值。
- 非法值就地提示，不引入多层确认弹窗。
- 高级分支不能把标准分支无效结果自动提升为有效，也不能静默替换标准结果。

### 多图片工作区

- 文件选择支持单选和多选。每张图片拥有独立工作项状态：输入身份、ROI 草稿、背景、标定、预览、正式记录、需要重新计算、质量警告和导出状态。
- 当前工作项优先完成显示和预览；其余工作项可以低优先级准备，但不得阻塞当前 UI。
- 切换图片恢复该图片状态，不把上一张的 ROI、标定或结果静默复制过来。
- ROI 可通过显式锁定应用到后续图片；标定可通过显式操作应用到已选批次。
- 后台任务带工作项和请求身份；迟到事件不能覆盖已经切换或修改后的工作项。
- 单图失败、取消或超时不终止其他图片。
- 本轮不持久化整个临时工作区；关闭程序后预览和未导出草稿可以丢弃，正式报告和诊断资产由用户保存。

### WPF 工作区和交互

- 工作区采用左侧工作流配置、中央主要图像、右侧结果与报告、下方 X/Y 剖面和累计能量曲线的布局。
- 主要视觉使用淡绿色应用背景、白色面板、深色文字和一致强调色；黑色限定于图像显示框。
- 中文文本需要时允许调整控件宽度、间距和换行；不得为像素级模仿 Prototype 截断中文。
- 中央图像支持输入、校正、模型和残差等实际可用图层；未产生的图层禁用并说明原因。
- 显示工具支持灰度/伪彩、自动分位数/全范围/手动范围、平移、缩放、恢复视图和叠加开关。
- 显示设置与分析配置严格分离，不产生重新计算状态。
- ROI 支持框内拖动、图像单击重定位、边角缩放、数值编辑、方向键微调和修饰键大步移动。
- ROI 拖动期间视觉反馈实时；完整预览依据性能在拖动中节流更新或鼠标释放后更新。
- 图像内部不放置遮挡数据的界面说明、图例或普通操作控件。
- 结果区域使用核心指标卡、质量与解释区域以及折叠诊断，不直接把英文机器字段拼成主结果文本。
- Tab 顺序符合视觉工作流；状态同时使用文字、图标和颜色；主要控件具有中文无障碍名称和帮助文本。
- 常用操作提供可发现快捷键。
- 状态视觉基于结构化状态枚举，不依赖中文或英文提示字符串匹配。
- 下拉框等显示文本与协议值分离，翻译不能改变业务判断。

### 报告和诊断

- 面向人的 PDF/PNG 报告使用简体中文主标签，保留标准缩写、单位、稳定机器身份和原始原因码。
- JSON 或诊断包保持稳定机器字段，不翻译协议键。
- 默认报告格式为 PNG，PDF 仍可选择；默认启用时间后缀。
- 默认报告名称包含输入图像名称和“焦斑分析报告”，经过文件系统安全处理。
- 报告源必须是正式分析记录；预览结果和需要重新计算的记录不能作为当前报告源。
- 正式记录即使整体或部分无效也可生成报告；报告首页显著标注状态，门控指标显示 N/A/null 和原因。
- 批量导出为每个正式结果生成独立报告，不在本规格中引入跨图综合统计。
- 无正式结果的工作项被跳过并进入导出汇总；单张失败不终止其他导出。
- 导出保持碰撞安全、原子写入和输入只读；成功后显示实际路径。
- 诊断导出继续由用户显式决定是否附带原图。

### 性能、窗口和发布

- 对主要 `1600×1200` 输入，当前图片目标在 1 秒内显示可操作输入图。
- ROI 和画布操作不得被同步分析阻塞 UI 线程。
- ROI 操作完成后，常规预览目标在 2 秒内更新；更慢时显示明确进度并允许取消或切换。
- 当前图片处理优先于其他工作项后台准备。
- 主要设计目标为 `1920×1080`，同时保证 `1366×768` 可以完成全部主流程。
- 支持 Windows 100%–200% DPI；中文文本不重叠、截断或隐藏关键操作。
- 小窗口可以使用分隔条、面板折叠和局部滚动，但不依赖固定全屏。
- 发布仍为无需管理员权限和开发环境的 one-folder 便携客户端，不改变已接受的进程与部署边界。
- 最终验收针对 fresh extraction 的发布包，覆盖中文、空格、较长路径及主要真实 PNG。

### 规格治理

- 本规格扩展并替代 Issue #66 中“显式确认配置后再运行”“只支持单图”“只支持灰度 PNG”和旧报告导出门控等产品工作流假设；#66 继续作为 Prototype A WPF 工作区和交互实现子项，不应另建第二套 UI。
- 本规格替代 Issue #6 及 `analysis-contract-v1` 中“首次 ROI 固定为全图”“计算前逐项确认 ROI/强度语义”和“第一阶段只支持单张输入”的产品交互条款。最终人工确认仍冻结正式分析使用的同一 ROI 和测量语义，不放松测量审计。
- 本规格更新 Issue #9 中“所有指标使用用户确认 ROI”的界面流程解释：预览可使用自动候选 ROI，正式记录仍只使用最终确认 ROI。Issue #9 的科学计算域、质量状态、解析 oracle、数值容差、原因码及确定性要求保持有效。
- ADR-0001 的 WPF 外壳、独立 Python 分析核心/worker、版本化 NDJSON、显示与测量分离、不可变 `AnalysisRecord` 和不在 C# 重写科学算法的边界全部保留。
- 自动预览、最终人工确认、无效正式报告及多工作项状态采用新的版本化契约，不静默改变旧分析记录的解释。
- 为该工作流新增 ADR，记录减少操作负担与保持测量审计之间的权衡。
- 实现可以拆分为多个子 Issue，但所有子项和真实发布包验收完成前，总 Issue 不得关闭。

## Testing Decisions

### 最高层测试 seam

- 本规格主要自动化 seam 是单一高层多图片工作区 presentation seam。
- 该 seam 接收用户意图、输入解码结果、自动候选结果、worker 生命周期事件、分析记录、显示投影和导出结果；产出外部可观察的工作项列表、当前工作项、ROI 草稿、预览/正式状态、可用操作、图层、质量提示、需要重新计算状态和导出状态。
- 绝大多数工作流测试通过该 seam 完成，不分别测试按钮事件、私有 helper 或具体控件树结构。
- presentation seam 只编排，不重新实现输入、ROI 检测、背景估计、指标或质量规则。

### 现有领域 seam 的分工

- 输入适配器 seam 验证 PNG 解码策略、等通道 RGB、位深、哈希和结构化拒绝原因。
- 分析核心 `analyze` seam 验证自动背景、最终 ROI、预处理、指标、质量状态、正式分析记录及确定性，不通过 UI 测试重复验证公式。
- worker interface seam 验证 WPF 和 CLI 使用相同请求、事件和结果语义，以及取消、超时、崩溃和协议错误。
- 显示投影 seam 验证图层、曲线、色图、范围和叠加；显示改变不得修改分析记录。
- 报告 seam 验证中文内容、无效状态、PNG/PDF、名称、时间后缀、碰撞安全和批量独立输出。
- 少量真实 WPF 自动化与人工验收证明 XAML 绑定、鼠标/键盘、布局、DPI 和发布包真实连接到 presentation seam。

### 测试原则

- 测试外部可观察行为，不断言私有实现、控件字段名、事件调用次数或源码字符串存在。
- 状态测试分别断言流程状态、预览/正式状态、指标有效性、需要重新计算和导出门控。
- 科学测试分别断言对外值、单位、状态和全部原因码；计算成功不能替代测量有效断言。
- 真实 PNG 验证解码、身份、重复性和工作流，不把缺少物理真值的采集图当作绝对数值 oracle。
- 合成输入用于主焦斑、多候选、坏点、背景、饱和、裁边和检测失败的确定性 oracle。
- 性能测试记录机器、构建、输入尺寸和运行模式，不以主观“流畅”替代数据。
- 自动化不能替代最终人类对 Prototype、中文界面和便携发布包的验收。

### 输入测试

- 覆盖 8 位灰度、16 位灰度和严格等通道 8 位 RGB PNG。
- 覆盖大写和小写扩展名、中文路径、空格路径和较长路径。
- 对三张主要真实 PNG 断言 WPF/Python/worker 解码成功、强度数组一致、原始三通道事实保留。
- 覆盖通道不一致 RGB、RGBA、调色板、损坏 PNG、无效位深和未支持编码的结构化拒绝。
- 验证不发生 Gamma、ICC、加权灰度或位深转换。
- 验证输入只读、哈希和时间戳不变。

### 自动 ROI 和背景测试

- 合成单峰验证主焦斑候选、ROI 包含目标和安全边距。
- 孤立热像素场景验证热像素不会取代积分能量主焦斑。
- 双峰和多峰验证主候选选择、能量比诊断和非阻断警告。
- 检测失败或低信号场景验证居中回退区域和回退状态。
- 边界焦斑验证 ROI 保持范围内并产生截断警告。
- 自动背景验证与 ROI 不重叠、排除坏点/饱和/正信号并满足空间支持。
- 背景不足验证不会静默全图中值兜底，相关指标按档案降级。
- 相同输入和档案重复运行得到一致候选、背景和诊断。

### 工作区 presentation 测试

- 覆盖空工作区、单图、多图、当前图切换和后台准备。
- 覆盖打开后自动进入预览，不需要中间确认。
- 覆盖最终确认同时提交 ROI 和相对强度语义，并生成正式记录。
- 覆盖预览不能导出、正式可以导出、需要重新计算不能作为当前报告源。
- 覆盖 ROI 移动、缩放、单击重定位、数值编辑和键盘调整共享同一状态。
- 覆盖未锁定 ROI 不跨图继承，显式锁定后按规则应用。
- 覆盖标定默认单图和显式批量应用。
- 覆盖参数、ROI 或标定修改使旧正式记录过期并产生新预览。
- 覆盖旧记录保留，新确认产生新记录身份。
- 覆盖单图失败、取消或迟到 worker 事件不影响或覆盖其他工作项。
- 覆盖结构化状态驱动颜色和文案，不依赖具体语言字符串。
- 覆盖显示设置改变不产生需要重新计算。
- 覆盖合法和非法高级设置、偏离默认提示及恢复默认。

### WPF 交互和视觉测试

- 真实窗口验证三栏加底部曲线结构、主要操作位置及中央图像空间。
- 驱动鼠标拖动、边角缩放、单击重定位、滚轮缩放、画布平移和恢复视图。
- 驱动键盘 Tab、ROI 方向键和快捷键。
- 验证输入、校正、模型、残差图层的启用与不可用原因。
- 验证灰度、伪彩、显示范围和叠加不会改变记录身份或指标。
- 验证核心指标卡、质量解释、折叠诊断和无效值 N/A。
- 验证未批准英文不出现在面向用户的窗口、提示、图例和报告中。
- 在 `1920×1080`、`1366×768` 及 100%–200% DPI 下验证无关键控件不可达、中文不重叠。
- 以 Prototype A 并排截图进行人工评审；截图是辅助证据，不替代状态和交互断言。

### 报告和多图测试

- 验证默认 PNG、可选 PDF、默认时间后缀和包含输入名的中文默认报告名。
- 验证有效、需复核、部分无效和整体无效正式记录均如实生成报告。
- 验证无效指标不泄露内部尝试值，中文说明和原始原因码同时存在。
- 验证批量导出每图一份、跳过无正式结果、单图失败不中止其余项，并生成汇总。
- 验证 stale/预览记录不能冒充当前正式报告源。
- 验证碰撞安全、原子写入、输入只读和实际路径反馈。
- 验证 JSON/诊断机器键保持稳定。

### 性能和发布验收

- 在声明环境中测量输入首次显示、预览更新时间和后台处理影响。
- 验证耗时操作不阻塞 UI dispatcher，取消和切换保持响应。
- 从 fresh extraction 启动实际 self-contained 便携包，不依赖 Python、.NET SDK、管理员权限或网络。
- 使用三张主要真实 PNG 完成多选、自动预览、ROI 操作、正式分析和批量导出。
- 在中文、空格和较长输入/输出路径下完成流程。
- 记录客户端身份、构建清单、测试日期、截图、性能数据、记录 ID、质量状态、导出路径和限制。
- 人工验收者明确签署结果；代理不能代替人类验收。

## Out of Scope

- `.RAW`、JPEG、TIFF、BMP 及未明确列入的其他输入格式；
- 对真正彩色 RGB 执行加权灰度或颜色科学转换；
- 自动推断真实物理空间标定；
- 把 Prototype 演示标定当作设备校准；
- 相机实时采集、流式处理和实时视频；
- 无人值守地为全部图片生成正式测量结果；
- 跨图片统计、趋势分析或综合批次报告；
- 工作区项目文件、关闭后自动恢复和长期会话持久化；
- 多 ROI、旋转 ROI、多边形 ROI 或三维分析区域；
- 自动改变用户已看到或已确认的 ROI 以改善结果；
- 未冻结的超高斯、Gamma 测量、“仅测量”等 Prototype 演示算法；
- 独立绘图模式、论文图编辑器和 Prototype B/C；
- 将 HTML Prototype、WebView2 或本地 Web 服务用于生产 UI；
- 在 C# 中复制 Python 科学核心；
- 改变已冻结的 FWHM、D4σ、EE、集中度等指标数学定义；
- 把 provisional 分析档案升级为 validated；
- 云同步、账户、远程服务或协作；
- 安装器、代码签名和自动更新；
- 以完全像素一致作为唯一 Prototype 验收方法；
- 删除或覆盖旧分析记录。

## Further Notes

- 本规格与开放的 Issue #66 高度重叠，但范围更广，并显式改变 #66 保留的旧工作流假设。#66 继续作为 Prototype A 的 WPF 工作区、显示工具、交互式 ROI 和 presentation seam 实现子项；不应并行实现另一套工作区。
- #66 中“显式确认配置后再启动分析”“只支持灰度 PNG”“单张输入”“无效结果旧导出门控”等内容由本规格替代。实施前应在 #66 中链接本总规格并标明被覆盖条款。
- Issue #6 的科学指标、质量门控、同一最终 ROI 计算域和不可变记录原则继续有效；被替代的是全图首次候选、逐项确认和单图产品流程。Issue #9 的验证 oracle、容差、质量状态及确定性要求继续有效；预览允许自动候选，正式结果仍使用最终确认 ROI。
- ADR-0001 仍是架构依据：WPF 外壳、独立 Python worker/core、版本化 NDJSON、显示与测量分离、不可变分析记录和不在 C# 重写科学核心均不改变。
- 现有冻结分析契约不应被原地无版本地改写。新规格应声明后继契约、档案或产品工作流版本，并保证旧记录继续按旧版本解释。
- 自动主焦斑检测和 ROI 生成需要在版本化档案中固化参数，并通过代表性图像验证；本规格冻结外部行为，不凭主观值立即冻结阈值。
- 主要真实目录中的三张 PNG 都是等通道 RGB；它们证明必须支持此容器形式，但不证明传感器线性响应、物理标定或绝对测量真值。
- 默认自动化不能隐藏科学限制。用户操作减少，但背景来源、相对强度声明、档案版本、ROI、质量状态和原因仍进入正式记录与报告。
- 总工作可拆为输入契约、自动 ROI/背景、自动预览与正式记录、多图片状态、#66 WPF 工作区、默认档案与标定、报告导出、规格文档和发布验收等子 Issue；全部子项完成并通过真实便携包验收后，总 Issue 才可关闭。

## Contract identity and transition

本规格与 ADR-0002 共同定义 `workflow-contract-v1`：它负责输入后的自动预览、分析区域（ROI）草稿、最终人工确认、正式结果、需要重新计算和多图片工作项状态。它不取代 `analysis-contract-v1`、`standard-profile-v1`、`quality-profile-v1` 或 `validation-contract-v1`；这些标识继续解释测量数据、质量状态、指标公式、oracle、容差和确定性。

`workflow-verification-contract-v1` 是本工作流的验证契约。它要求通过高层 presentation seam、输入适配器、分析核心、worker、显示投影和报告 seam 验证外部行为，并以真实 WPF/便携包人工验收补足鼠标、键盘、DPI、中文布局和发布环境证据。它不以源码字符串检查替代行为证据，也不把本规格中的目标时延当作已经测得的性能事实。

任何改变工作流状态、输入语义、报告门控或机器协议的变更都必须产生新的契约/版本并保留迁移边界。旧 `AnalysisRecord` 仍按生成时的 `analysis-contract-v1`、profile、算法和导出版本解释；新工作流不得静默重判旧记录或覆盖旧报告。`analysis-request-v1`、`analysis-event-v1`、`analysis-result-v1` 等机器接口标识保持稳定，除非另有带版本的协议决议。

### Delivery issues

- [ ] #66 — 完成 Prototype A 中文 WPF 工作区、显示工具与交互式 ROI
- [ ] #76 — 统一 PNG 输入契约并支持等通道 RGB 真实样例
- [ ] #77 — 实现版本化主焦斑检测、自动 ROI 与自动背景候选
- [ ] #78 — 重构自动预览、最终确认与正式分析记录工作流
- [ ] #79 — 实现多图片工作区状态与后台调度
- [ ] #80 — 统一版本化默认档案、未标定像素域和高级设置
- [ ] #81 — 支持中文无效结果报告和多图片批量导出
- [ ] #82 — 更新自动化工作流规格、验证契约和中文用户文档
- [ ] #83 — 执行 Prototype、性能和便携发布包整体验收
