---
issue: 82
status: documentation-contract
workflow_contract: workflow-contract-v1
verification_contract: workflow-verification-contract-v1
science_contract: analysis-contract-v1
validation_contract: validation-contract-v1
parent: 75
adr: ADR-0002
---

# Issue #82：自动工作流后继规格、验证契约与中文用户文档

## 1. 目的和非目标

本文把 Issue #75 与 ADR-0002 落成可引用、可验证的版本化文档。它定义产品工作流和用户可观察行为的边界，不实现分析代码、WPF 控件、worker、报告生成器或新的测量算法。

科学计算语义继续由 `analysis-contract-v1`、`standard-profile-v1` 和 `quality-profile-v1` 定义；验证方法继续由 `validation-contract-v1` 定义。本文不重新定义 FWHM、D4σ、EE、集中度、质量阈值、解析 oracle、数值容差或物理标定，也不把任何 profile 从 `provisional` 升级为 `validated`。

## 2. 契约身份

| 契约 | 版本 | 责任 | 历史处理 |
| --- | --- | --- | --- |
| 测量科学契约 | `analysis-contract-v1` | 输入数据语义、计算域、指标公式、质量状态、报告门控、记录解释 | 保留；旧记录按记录中的版本解释 |
| 参数档案 | `standard-profile-v1` / `quality-profile-v1` | 默认参数和质量规则 | 保留 `provisional`，不因本 Issue 改变 |
| 验证契约 | `validation-contract-v1` | oracle、容差、场景、确定性和 profile 升级门槛 | 保留；后继工作流增加验证范围，不放宽科学门槛 |
| 产品工作流契约 | `workflow-contract-v1` | 自动预览、ROI 草稿、正式结果、多图片工作项和需要重新计算 | 新增；由 Issue #75/ADR-0002 承载 |
| 工作流验证契约 | `workflow-verification-contract-v1` | seam、用户可观察行为、证据边界和发布验收 | 新增；由本文承载 |
| 机器协议 | `analysis-request-v1`、`analysis-event-v1`、`analysis-result-v1` | WPF、worker 和调用者之间的稳定英文字段和值 | 保持标识和代码值，不翻译 |

工作流版本与测量版本独立。工作流实现升级不得改变旧 `AnalysisRecord` 的科学解释；测量公式、阈值、单位或状态含义变化时，必须按 Issue #6/#9 的规则生成新的科学契约/profile/算法版本。

## 3. 明确的后继与被替代条款

### 3.1 被 `workflow-contract-v1` 替代的产品流程

| 历史位置 | 被替代的产品假设 | 后继规则 |
| --- | --- | --- |
| Issue #6：首次 ROI 固定为全图 | 初始区域必须使用全图 | 自动提出版本化 ROI 候选；仍为单个、轴对齐、完全在图像内的矩形 |
| Issue #6：计算前逐项确认 ROI/强度语义 | 必须先逐项确认后才可运行 | 图片载入后自动产生预览；最后一次确认同时确认 ROI 和相对强度码值语义 |
| Issue #6：第一阶段只支持单张输入 | 工作区不能同时管理多图 | 可以选择多图；每张图拥有独立草稿、预览、正式记录、质量和导出状态 |
| Issue #9：所有指标均只能在用户确认流程后开始 | 旧产品交互对验证启动时机的描述 | 预览可用自动候选；正式记录仍必须使用最终确认的同一 ROI，科学指标和质量门控不变 |
| Issue #66：显式配置确认、单图和旧报告门控 | 旧 Prototype 工作流假设 | 由 #75/ADR-0002 的自动预览、正式结果和批量独立导出替代；#66 的 WPF/展示实现范围仍有效 |

“替代”只针对产品交互和工作流时序。Issue #6/#9 的输入不静默转换、signed 校正强度图、指标数据源、质量状态、`null` 门控、确定性、历史记录和真实图像不证明物理真值等条款继续有效。ADR-0001 的 WPF 外壳、独立 Python 核心/worker、版本化 NDJSON、显示与测量分离和不可变记录边界继续有效。

### 3.2 历史记录和历史证据

已有记录保留原 `record_id`、契约/profile/算法版本和状态。新工作流不得重新判定、覆盖、静默重算或删除旧记录。用户改变输入、ROI、标定、预处理、模型或允许调整的参数时，旧正式结果进入“需要重新计算”，新预览和新确认产生新的记录身份。

已有验证文件是当时运行的证据，不是新工作流已经运行的证明。历史文件中的 `PASS`、`NOT RUN`、输入排除、哈希、环境、时间和限制必须保持原意；后继文档应说明适用版本，而不能用改写历史表格的方式制造新证据。

## 4. `workflow-contract-v1` 外部行为

### 4.1 打开输入到预览

1. 用户选择一个或多个受支持 PNG；当前工作项优先显示输入图像。
2. 系统自动解码、执行当前推荐档案的默认预处理、选择 ROI 外背景样本、定位主焦斑并提出 ROI。
3. 系统产生预览结果。预览可以显示数值和质量状态，但不能导出为测量报告，也不能伪装成正式结果。
4. 定位置信度低、背景支持不足或指标质量不满足时，应显示非阻断中文说明和原因码；只有解码失败、参数无效或完全不能产生结构化结果才阻断相应操作。

自动候选一旦展示，不得为了改善数值而静默改变。新输入、配置变化或用户操作才可以产生新候选，并应保留变化原因。

### 4.2 ROI 操作和正式结果

ROI 是唯一规范的“分析区域”术语。它必须是单个轴对齐矩形、完全落在输入图像内；用户可以框内拖动、单击重定位、拖动边角缩放、数值编辑，并使用方向键微调、修饰键大步移动。所有操作共享同一个 ROI 草稿，不维护互相矛盾的坐标副本。

用户只在最终操作中确认当前 ROI 和“按相对强度码值测量”的语义，生成正式分析记录。正式只表示用户确认了配置，不表示测量有效。正式记录即使包含 `caution`、`invalid` 或 `unavailable` 指标，也必须保留完整质量原因并按报告契约门控。

### 4.3 标定和单位

没有可信空间标定时，预览和正式分析仍可使用 `px`。物理域指标必须显示未标定或不可用，不得使用 `1 mm/px`、Prototype 演示值或其他占位值。x/y 标定的值、单位、来源和 `confirmed`/`provisional`/`missing` 状态必须记录；显式批量应用才可以作用于已选图片，并使受影响的旧正式结果需要重新计算。

### 4.4 多图片、报告和诊断

每张图片的输入身份、ROI、标定、预览、正式记录、质量警告、需要重新计算和导出状态独立保存。切换图片不得静默继承上一张的草稿；ROI 锁定和批量应用标定必须是显式操作。单张失败、取消或超时不得终止其他工作项。

报告源只能是当前配置下的正式结果；预览和需要重新计算的记录不能作为当前报告源。每张正式结果单独导出 PNG（默认）或 PDF；批量导出跳过没有正式结果的工作项并记录汇总，单张导出失败不阻止其他项。无效指标输出 `N/A`/`null` 和原因，不输出内部尝试值。

默认诊断包保存配置、输入身份、状态、有效性、原因码、record ID、fingerprint、版本和失败详情，但不附带原图。只有用户明确选择附件时才复制原图，并应在证据中记录该意图。机器键、协议值、路径、文件名和哈希保持稳定英文/原样；面向人的主标签使用简体中文。

## 5. `workflow-verification-contract-v1`

### 5.1 验证层次

验证按以下 seam 分工，避免用界面字符串或源码结构代替行为测试：

1. **输入适配器 seam**：8/16 位灰度 PNG、严格等通道 8 位 RGB PNG、大小写扩展名、中文/空格/长路径、哈希、只读和结构化拒绝。
2. **分析核心 seam**：自动背景/主焦斑/ROI 候选和最终 ROI 的输入；复用 `validation-contract-v1` 的指标、质量状态、原因码和确定性断言。
3. **worker seam**：稳定的 `analysis-request-v1`、`analysis-event-v1`、`analysis-result-v1`，以及取消、超时、崩溃和迟到事件隔离。
4. **presentation seam**：单图、多图、当前工作项优先、预览/正式/需要重新计算、ROI 操作、标定作用域和导出门控等外部状态。
5. **显示投影 seam**：图层、伪彩、拉伸、插值、缩放、平移、叠加和曲线；显示设置不改变 `AnalysisRecord`。
6. **报告/诊断 seam**：中文标签、稳定机器字段、PNG/PDF、批量独立输出、无效值门控、诊断隐私和附件意图。
7. **真实 WPF/便携包验收**：只用来补足真实绑定、鼠标/键盘、DPI、布局、中文路径、fresh extraction 和标准用户环境证据。

科学测试仍须分别断言流程状态、指标有效性、对外值和全部原因码。Issue #9 的合成 oracle、容差、低 SNR 固定种子、profile 封顶和 `provisional` 升级门槛不因工作流自动化而放宽。

### 5.2 用户可观察验收矩阵

| 区域 | 必须观察的行为 | 证据类型 |
| --- | --- | --- |
| 输入 | 灰度与严格等通道 RGB 成功；彩色 RGB、RGBA、调色板、损坏输入有结构化拒绝 | 自动化结果 + 输入身份 |
| 自动预览 | 打开后自动显示可操作图像、ROI 候选和预览；无逐项确认门槛 | presentation seam；必要时真实窗口 |
| ROI | 拖动、缩放、重定位、数值编辑、键盘移动共享同一状态且不越界 | presentation seam + 人工交互 |
| 正式结果 | 一次最终确认生成正式记录；正式不等于有效；预览不可报告 | presentation/report seam |
| 标定 | 缺失时像素域可用、物理域明确不可用；confirmed/provisional/missing 可追溯 | 核心/报告结果 |
| 质量 | `valid`、`caution`、`invalid`、`unavailable` 和原因码分开显示；不泄漏门控值 | 核心/报告结果 |
| 多图 | 状态隔离、当前项优先、显式锁定/批量标定、单项失败隔离 | presentation seam |
| 报告 | 正式结果逐图导出，PNG 默认、PDF 可选、碰撞安全、输入只读 | report seam + 文件身份 |
| 诊断 | 默认无原图；显式附件才包含原图；机器键不翻译 | diagnostics seam + archive listing |
| 发布 | fresh extraction 可启动且不依赖 Python/.NET/编译器/管理员权限；配置目标环境 | 人工/发布证据 |

每条自动化测试应验证外部行为和稳定契约值，不检查私有控件名称、事件调用次数或源码中是否存在某个文案。性能目标只能在记录机器、构建、输入尺寸和运行模式后作为测量结果报告，不得从未运行的规格目标推断通过。

### 5.3 证据与程序边界

- **程序**是可重复执行的步骤、输入、预期行为和证据路径；它不等于结果。
- **运行证据**是带版本、时间、环境、输入哈希和实际输出的记录；没有运行证据的复选框必须保持 `NOT RUN`。
- **自动化通过**只证明其覆盖的 seam；不能替代中文 UI、鼠标/键盘、DPI、clean-machine 或用户验收。
- **真实图像证据**可证明解码、身份、重复性、状态和警告；缺少物理真值时，不证明绝对尺寸、物理饱和或光学成因。
- **profile 验证**只有在 Issue #9 的全部合成/真实/接口门槛完成并经指定审查后才可升级；#82 不执行升级。

## 6. 文档交叉引用和维护规则

- 本文件引用 `issue-75-spec.md` 和 `docs/adr/0002-automatic-preview-and-formal-results.md` 作为产品工作流来源。
- `issue-6-spec.md` 和 `issue-9-spec.md` 顶部保留后继规格通知；其科学章节不得因中文用户文档而改写。
- `docs/research/issue-4-representative-inputs.md` 中历史输入盘点保持原有“当时未纳入灰度回归”的事实；后继支持等通道 RGB 是产品契约，不把旧排除记录改写成旧测试已经覆盖。
- `docs/validation/issue-65-human-acceptance.md`、`packaging/ALPHA-TRIAL-ACCEPTANCE.md` 和 `docs/validation/issue-74-packaged-release.md` 必须区分待执行程序与已经观察到的运行证据；修改验收程序不等于补充运行证据。
- 面向用户的文档使用简体中文；`ROI`、`FWHM`、`D4σ`、`EE`、`PNG`、`PDF` 和机器字段保留稳定拼写，首次出现时可以附中文说明。

## 7. 接受条件

- [ ] 后继工作流和验证契约使用明确版本，并引用 #75、ADR-0002、#6、#9 与 ADR-0001。
- [ ] 被替代的产品交互条款和继续有效的科学/架构条款均有明确清单。
- [ ] 中文用户文档覆盖支持输入、自动预览、ROI、标定、质量、报告、诊断和多图导出。
- [ ] 机器协议标识、代码值、字段名和资产身份规则保持英文稳定。
- [ ] 历史验证记录的事实、哈希、状态和未运行边界未被冒充为新工作流证据。
- [ ] 文档检查验证交叉引用和用户可观察契约；行为测试归属于对应输入、工作流、报告和发布 Issue。
- [ ] `standard-profile-v1` 与 `quality-profile-v1` 仍为 `provisional`，没有新增科学数值承诺。
