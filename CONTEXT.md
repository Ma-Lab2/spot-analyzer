# 焦斑分析

本上下文描述从实验焦斑图像获得可追溯空间测量结果时使用的领域语言。指标的具体数学定义将在 Wayfinder 研究与决策票中确定后补充。

## Language

**输入图像**：
从受支持文件解码得到、尚未进行背景扣除、坏点修复、滤波、裁剪或阈值处理的强度数据。
_Avoid_: 原始图（当数据已经过任何校正时）

**校正强度图**：
输入图像经过已记录的测量预处理后形成、供指标计算使用的强度数据。
_Avoid_: 原始图、显示图

**显示图像**：
为便于观察而渲染的图像；可以采用色图、插值或明确标示的显示平滑，但不作为测量指标的数据来源。
_Avoid_: 测量图、校正强度图

**空间标定**：
像素坐标与物平面空间单位之间的对应关系，分别由 x/y 方向的长度每像素表示，并记录其来源。
_Avoid_: 像素尺寸（未说明是传感器面还是物平面时）

**分析区域**：
围绕待测焦斑选定、用于预处理、拟合或能量积分的图像区域；其边界是否截断信号属于测量有效性的一部分。
_Avoid_: 裁剪框、窗口（未说明用途时）

**计算成功**：
分析过程产生了某个数值或结构化结果；它不表示该结果满足测量质量条件。
_Avoid_: 测量有效

**测量有效性**：
根据输入质量、分析区域、模型适用性和研究确定的质量条件，对结果是否可用于解释作出的判断。
_Avoid_: 计算成功、拟合收敛

**预处理**：
对输入图像进行、并且需要被记录的测量准备步骤，例如背景处理、坏点处理、滤波、裁剪或阈值处理；其输出可以形成校正强度图。
_Avoid_: 显示图像的视觉渲染

**分析模型**：
用于从校正强度图和分析区域计算测量结果的、具有明确名称、版本和参数的分析方法。
_Avoid_: 显示图像的色图或插值方式

**测量报告**：
对一次分析结果及其解释所需上下文的可保存记录，至少关联输入图像、空间标定、预处理、分析模型、测量结果和测量有效性。
_Avoid_: 只包含数值或只包含显示图像的导出文件

**报告名称**：
由用户为测量报告指定的名称；可以选择附加生成时间，以区分同名报告并保留创建时点。
_Avoid_: 输入图像文件名（除非用户明确将其作为报告名称）

**分析配置**：
一次分析实际采用的输入引用、空间标定、预处理、分析区域、分析模型及其版本和参数的完整声明。
_Avoid_: 显示设置、临时 UI 状态

**分析记录**：
一次分析计算尝试的不可变机器可读记录，关联输入图像、分析配置、指标、测量有效性、流程状态和可追溯派生资产；重新计算生成新记录，不覆盖旧记录。
_Avoid_: 只有数值的结果、可变的工作区结果

**验证完成**：
针对已声明的算法验证范围，合成与真实代表性图像、性能、可解释性和报告证据均满足验收条件；不等同于生产发布完成。
_Avoid_: 代码实现完成、计算成功

**代表性图像**：
用于验证算法行为、可解释性和失败规则的受控合成图像或真实采集图像；真实图像必须具备可追溯来源，缺失验证资产不能被当作验证通过。
_Avoid_: 任意样例图、显示图像

**性能基线**：
在声明的硬件、图像尺寸和运行模式下，对核心分析及端到端 worker 行为设定并实测的耗时目标；只对声明的验证环境作出结论。
_Avoid_: 测试套件总耗时、未定义环境的“足够快”

**用户验收**：
用户对 Issue 所声明范围、验收证据和是否需要修改规格作出的明确决定；没有该决定，Issue 不能仅凭代码和自动化测试视为完成。
_Avoid_: 测试通过、profile 验证通过

**便携客户端**：
无需目标机器预先安装开发环境、管理员权限或网络服务即可移动、启动和使用的应用交付物；其分析行为必须与受控验证环境中的分析记录保持一致。
_Avoid_: 仅能在开发环境启动的程序、只包含数值结果的脚本

**发布包**：
为便携客户端准备的、具有明确版本、依赖、算法版本和文件身份的可分发整体；发布包的存在不等同于算法 profile 已完成正式验证。
_Avoid_: 临时构建目录、未记录依赖的压缩文件

**流程状态**：
描述一次分析流程是否正在处理、已完成计算、已导出或因输入、参数、分析错误而终止；不表示测量结果是否可信。
_Avoid_: 测量有效性、指标有效性状态

**指标有效性状态**：
对单个测量指标是否满足其解释条件的判断，取值包括有效、警告/需复核、无效和不适用/不可用；与流程状态分开记录。
_Avoid_: 计算成功、流程状态

**质量原因码**：
解释指标或分析为何降级、无效或不可用的稳定机器可读标识；可以有多个，并随分析记录保存。
_Avoid_: 面向人的临时提示文字

**规范档案（profile）**：
为一组分析方法规定参数、质量条件和状态门控的版本化定义；其验证状态可以是 provisional 或 validated，但验证状态不改变档案本身的版本身份。
_Avoid_: 临时 UI 参数、显示设置

**规范中心**：
由分析流程确定并记录、用于指标计算和质量诊断的焦斑中心；它与拟合中心、质心等诊断值可以不同。
_Avoid_: 任意显示标记、未记录的峰值位置

**显示图层**：
从输入图像或校正强度图渲染、供用户观察和解释的图层；其色图、拉伸、插值和叠加不改变测量数据或分析记录。
_Avoid_: 校正强度图、测量数据

**需要重新计算**：
表示当前分析记录所依据的输入或分析配置已不再与工作区当前选择一致，因此结果不能继续作为当前配置的结果使用。
_Avoid_: 测量有效性、分析失败
