# 规范分析契约与测量有效性规则

## 状态

- **契约版本**：`analysis-contract-v1`
- **标准参数档案**：`standard-profile-v1`
- **质量参数档案**：`quality-profile-v1`
- **初始验证状态**：`provisional`
- **适用阶段**：MVP 第一阶段

只有 `standard-profile-v1` 和 `quality-profile-v1` 通过本规范规定的真实与合成回归验收后，档案状态才可升级为 `validated`。档案处于 `provisional` 时，任何单次分析最多只能得到 `caution`，不得声明为规范 `valid`。

## 1. 目标与范围

本规范冻结焦斑分析已经确认的标准流程、数据语义、默认参数、可调参数、指标定义、质量状态、报告门控、历史记录和对比兼容性，作为后续实现和验收的共同契约。第 13 节列出的结果相关实现细则仍须固化进版本化 profile；在这些细则完成且回归通过前，本契约可指导实现，但不得被宣称为已验证、跨实现完全一致的测量标准。

第一阶段只支持：

- 单张输入图像；
- 单个由用户确认的矩形分析区域（ROI）；
- 单次分析记录；
- 单核心焦斑作为主分析对象。

不适用的输入、ROI 或测量模式必须显式拒绝，不得隐式套用本流程，也不得“尽量算出”一个可被误解为有效测量的数值。

本阶段必须保证：

1. 同一输入和同一完整配置重复运行得到一致结果；
2. 采用兼容契约和配置的不同图像可进行结构化指标比较。

不同设备、不同批次之间的可比性是后续校准与验证目标，不是本阶段的硬性科学承诺。

## 2. 规范术语与两层状态

### 2.1 流程状态

流程状态描述软件是否完成计算或导出，与测量是否可信分离：

- `processing`
- `input_invalid`
- `parameter_invalid`
- `analysis_failed`
- `computed`
- `exported`

解码失败、输入或参数无效、分析失败均不产生测量结果，也不得生成声称包含分析结果的正常报告。

### 2.2 测量有效性

每个指标独立拥有以下状态：

- `valid`：满足该指标的规范质量条件，可正常解释；
- `caution`：可报告数值，但必须显著展示全部原因码；
- `invalid`：已尝试计算但质量条件不成立，对外数值必须为 `null`；
- `unavailable`：该指标不适用或缺少计算前提，对外数值必须为 `null`。

`computed` 不等于 `valid`。拟合收敛也不等于测量有效。

整次分析另有摘要状态，并保留全部原因码。聚合时，核心指标的可报告严重性为 `valid < caution < invalid`；`unavailable` 表示不适用或缺少前提，不参与“最严重状态”比较，但若所有核心指标均为 `unavailable`，摘要为 `invalid`。某一模型不适用只影响依赖该模型的指标；只有没有任何可报告核心指标，或发生严重输入/流程错误时，整次分析才为 `invalid`。profile 的 `provisional` 上限只会把原本可为 `valid` 的摘要和指标封顶为 `caution`，不会把 `invalid` 或 `unavailable` 提升。

## 3. 标准测量流程

标准流程顺序固定为：

1. 解码与完整性检查；
2. 确认强度编码语义；
3. 在存在且采集条件匹配时执行暗场/平场校正；
4. 标记并掩膜坏点；
5. 优先使用匹配背景帧，否则使用经确认保护区上的稳健一阶仿射背景模型；
6. 仅在用户显式启用时执行噪声处理，并完整记录；
7. 生成并保留有符号浮点校正强度图及有效性、坏点、饱和和负值掩膜；
8. 在独立定位副本上确定规范中心；
9. 用户确认 ROI，并执行边界、采样、饱和、背景支持和多峰审查；
10. 计算非参数指标和旋转椭圆高斯拟合；
11. 对每个指标执行质量门控并生成分析记录与 PDF。

产品界面可以把上述步骤归入“背景扣除 → 坏点处理 → 分析区域处理”等粗粒度阶段，但不得改变算法执行顺序。坏点不得进入背景统计；背景区域必须与测量 ROI 分开记录。

### 3.1 输入编码语义

- 不得静默执行 gamma、sRGB、ICC 色彩转换、`16 bit → 8 bit` 转换或其他会改变测量语义的变换。
- 灰度图缺少 `gAMA`、`sRGB`、`iCCP` 等语义信息时，只有在用户确认“按相对强度码值测量”后才可继续，结果最多为 `caution`，不得声称输入是已确认的线性光强。
- RGB 仅在三个通道完全一致且该事实被记录时才可按灰度处理。
- 三通道不一致的彩色 RGB 默认拒绝，不得静默转灰度。
- 每次分析记录位深、端序（适用时）、通道、缺失元数据、解码 dtype、单位、输入哈希和读取状态。

### 3.2 暗场、平场与坏点

- 暗场和/或平场仅在用户提供且采集条件匹配时启用；缺失时不得伪造校正。
- 坏点默认只标记和掩膜，不自动修复。
- 默认不滤波、不执行 DPC。
- 滤波、DPC 或坏点插值修复属于显式高级分支，必须记录方法、核、边界模式、参数、版本、坏点数量及与标准分支的敏感性比较。
- 标准分支本身无效时，高级分支不得把结果“修复”为规范 `valid`。

### 3.3 背景来源与降级

背景来源优先级固定为：

1. 采集条件匹配的背景帧；
2. 经用户确认的背景子区域；
3. 系统提出、用户审查并确认的 ROI 外保护区。

背景帧必须记录曝光、增益、温度、光路、焦距、ROI、采集时间及其他可用匹配信息。

没有匹配背景帧时，标准降级流程为：

1. 使用经确认且独立于核心积分/拟合域的背景区域；
2. 排除坏点、饱和点、核心候选和明显正信号候选；
3. 拟合稳健一阶仿射模型 `B(x,y) = b0 + bx·x + by·y`；
4. 记录背景残差、有效像素数、排除像素数和 x/y 空间覆盖；
5. 有效背景像素数至少为模型参数数量的 10 倍，并且在 x、y 方向均至少跨越 3 个不同像素行/列；
6. 支持不足、疑似信号泄漏或残差过大时，依赖背景的指标为 `invalid`，不得自动改用全图中值。

单一全图中值不得作为静默默认兜底；若未来提供，只能作为显式、版本化的高级选项。

## 4. 数据、坐标、中心与 ROI

### 4.1 校正信号

保留背景校正后的有符号浮点数组 `S`，不得覆盖或静默截零。定义显式正信号：

```text
P = max(S, 0)
```

- 高斯拟合使用连续、有符号、未阈值化的 `S`；
- 能量、二阶矩和集中度指标统一使用 `P`；
- 无效像素不参与计算；
- 必须报告负值像素比例、负值总量及相关诊断。

像素物理面积为 `ΔA = Δx·Δy`。能量归一化分母为：

```text
T = Σ(P·ΔA)
```

### 4.2 坐标和角度

- 图像坐标：`x` 向右，`y` 向下；
- 角度从 `+x` 轴起，按图像坐标正方向记录；
- 角度范围固定为 `[0°, 180°)`；
- 主次轴顺序固定为 `major ≥ minor`；
- 非方形像元必须通过协方差/主轴的物理坐标变换计算物理尺寸，不得平均 x/y pitch；
- 同时保存像素域结果和可用的物理域结果。

### 4.3 规范中心

唯一规范测量中心为独立定位副本上的平滑峰中心：

- Gaussian `σ = 1 px`；
- 核截断到 `3σ`；
- 边界不环绕；
- 在局部峰邻域使用二次曲面进行亚像素定位。

定位副本只用于找峰，不得覆盖校正强度图。首次中心搜索在完整校正强度图上进行，独立于尚未确认的测量 ROI；所有测量指标仍从未平滑校正强度图计算。定位算法、参数和版本必须写入记录。用户确认的 ROI 必须包含规范中心；若不包含，系统必须要求调整 ROI 或重新定位，在此之前不得计算指标。

质心、二阶矩中心和高斯拟合中心均为诊断或模型参数，不替代规范中心。中心偏移必须记录并参与质量判定。

### 4.4 ROI

- 首次候选 ROI 默认为整幅图像；
- 用户必须显式确认 ROI 后才能计算；
- MVP 只支持矩形 ROI；
- ROI 必须完全落在输入图像内；
- 拟合、D4σ、EE 和集中度均使用同一用户确认 ROI；
- 不得为改善结果而静默缩小 ROI、扩大拟合域或扩大积分域；
- ROI 接触图像边界或窗口截断信号必须记录并影响相关指标状态。

## 5. 主模型与拟合契约

单核心主模型为旋转椭圆高斯：

```text
G = B + A·exp{-1/2[(u/σ1)^2 + (v/σ2)^2]}
```

其中 `u/v` 为旋转后的主轴坐标。

规范拟合要求：

- 数据：ROI 内连续、有符号、未平滑、未阈值化的 `S`；
- 域：仅用户确认 ROI，不隐式扩大；
- 残余背景 `B` 作为参数拟合并报告；
- 目标函数：有界普通最小二乘，作为确定性基线；
- 约束：`A > 0`、`σ1 ≥ σ2 > 0`，中心位于 ROI 内；
- 宽度下限为 `0.5 px`，上限由 ROI 最短边和采样支持约束；
- 初始化：中心使用规范平滑峰，`A` 使用峰值相对背景，`B` 使用背景估计，宽度由局部半高或 ROI 尺寸的版本化确定性规则给出；
- 不得先做正值截断或二值阈值化再拟合；
- 稳健损失只能作为显式高级选项，不能替换标准结果。

必须保存：初始化、全部参数、协方差/不确定度（若可得）、残差、优化器及工程参数、收敛状态、边界命中、迭代失败原因和算法版本。纯工程参数（如迭代上限和停止容差）可由实现固定，但必须可追溯、不可由用户任意改变，且不得改变结果语义。

## 6. 规范指标字段

每个指标字段必须携带：值、单位、状态、全部原因码、计算方法版本和 `null` 门控结果。

| 字段 | 唯一数据源 | 中心 | 域 | 定义 |
|---|---|---|---|---|
| `peak_center` | 完整校正强度图的独立定位副本 | 规范平滑峰 | 初次搜索独立于 ROI；确认 ROI 必须包含该中心 | 像素坐标；标定可用时附 x/y 物理坐标 |
| `gaussian_fwhm_major/minor` | 同一次高斯拟合 | 拟合中心为模型参数，规范中心不变 | 同一 ROI | `sqrt(8 ln 2)·σ`；像素和物理长度 |
| `gaussian_ellipticity` | 同一次高斯拟合 | 同上 | 同一 ROI | `major/minor ≥ 1` |
| `gaussian_angle` | 同一次高斯拟合 | 同上 | 同一 ROI | 主轴角 `[0°,180°)` |
| `moment_d4sigma_major/minor` | `P=max(S,0)` | 规范平滑峰 | 同一 ROI | 围绕规范中心的协方差特征轴标准差乘 4；像素和物理长度 |
| `moment_angle` | 同一正信号二阶矩 | 规范平滑峰 | 同一 ROI | 与规范坐标一致的主轴角 |
| `ee50/ee80` | `P=max(S,0)` | 规范平滑峰 | 同一 ROI 内圆域 | `EE(r)=Σ(P·ΔA)/T`；首次达到目标比例的半径 |
| `concentration_rref` | `P=max(S,0)` | 规范平滑峰 | 同一 ROI 内圆域 | `Σ(r≤Rref)(P·ΔA)/T`；仅在独立 `Rref` 明确时计算 |
| `fit_residual` | `S-G` | 拟合中心 | 同一 ROI | RMS、`L1/T`、背景噪声归一化残差 |
| `centroid_offset` | `P=max(S,0)` | 质心与规范峰中心比较 | 同一 ROI | 像素/物理距离及相对 `FWHM_major` 的归一化距离 |
| `quality_diagnostics` | 掩膜、背景、饱和、边界、峰检测 | 不适用 | 相应检查域 | 计数、比例、标志、支持量和原因码 |

补充规则：

- D4σ 围绕规范平滑峰中心计算；质心中心版本只能作为另名诊断字段。
- EE 按像素中心到规范中心的距离排序累积；跨越 50%/80% 时线性插值。
- 像素域半径使用 `sqrt((x-cx)^2+(y-cy)^2)`；物理域半径使用 `sqrt(((x-cx)·Δx)^2+((y-cy)·Δy)^2)`。非方形像元下不得用平均 pitch 把像素圆近似成物理圆。
- 圆形域默认按相应坐标域中的像素中心是否落入圆内判定；精确面积相交或径向重采样只能作为显式版本化选项。
- ROI 截断时，EE 可按当前窗口内的 `T` 计算，但必须明确标为 window-normalized，并按质量规则降级，不能冒充全空间包围能量。
- `Rref` 必须来自设计目标、仪器配置或实验协议，明确声明像素或物理单位，并在对应坐标域计算；不能由当前 FWHM 反推。缺少时 `concentration_rref=unavailable`。
- `concentration_rref` 不得称为 Strehl。
- 旁瓣/环检测和模型重叠度 `overlap` 暂不属于默认主指标，可作为版本化高级诊断。

## 7. quality-profile-v1

### 7.1 SNR

统一定义：

```text
SNR = (peak - background) / σ_background
σ_background = 1.4826·MAD
```

| 条件 | 状态 |
|---|---|
| `SNR ≥ 10` | 通过 |
| `5 ≤ SNR < 10` | `caution` |
| `SNR < 5` 或背景噪声不可估计 | 相关定量指标 `invalid` |

### 7.2 核心采样

| 条件 | 状态 |
|---|---|
| FWHM 两轴均 `≥3 px` | 通过 |
| 任一轴 `2–<3 px` | `caution` |
| 任一轴 `<2 px` | 高斯 FWHM 和 D4σ `invalid`；EE 在面积支持充分时最多 `caution` |

### 7.3 残差和中心一致性

必须保存：`R=S-G`、`RMS_residual=sqrt(mean(R²))`、`L1/T`、质心到规范中心距离、拟合中心到规范中心距离和参数边界命中。

| 条件 | 拟合类指标状态 |
|---|---|
| `RMS_residual/σ_background ≤2` 且中心偏移 `≤0.10·FWHM_major` | 通过 |
| 残差 `>2–≤4` 或偏移 `>0.10–≤0.25·FWHM_major` | `caution` |
| 残差 `>4`、偏移 `>0.25·FWHM_major`、未收敛或命中拟合边界 | `invalid` |

优化器返回成功本身不能放行拟合指标。

### 7.4 核心支持、边界与掩膜

定义：

- `P_peak`：规范中心附近峰值正信号；
- `core_mask`：ROI 内 `P ≥ 0.5·P_peak` 的连通区域中包含规范中心的区域；
- 核心裁边：`core_mask` 与 ROI 边界相交，或规范中心位于 ROI 边界；
- 接近边缘：核心到 ROI 边界的最短距离小于 `max(1 px, 0.5·FWHM_major)`。

| 核心掩膜有效像素比例 | 状态 |
|---|---|
| `≥0.95` | 通过 |
| `0.8–<0.95` | `caution` |
| `<0.8` | 核心尺寸指标 `invalid` |

`EE80` 在 ROI 最大可用半径内未达到时为 `unavailable`，并记录窗口截断。轻微接近边缘按指标降级；半高核心接触边缘时，高斯和 D4σ 指标为 `invalid`。

### 7.5 饱和

- 最大码值仅触发 `possible_saturation`，不得据此直接声称物理饱和；
- 只有输入元数据、采集信息或明确平顶/裁剪证据确认核心饱和时，才触发 `saturated_core`；
- `possible_saturation` 至少使相关指标为 `caution`；
- 确认核心饱和时，高斯 FWHM、D4σ、EE50/80 和 C(Rref) 等受幅值影响的定量指标均为 `invalid`，对外数值为 `null`；仅保留位置、掩膜计数和结构化原因。

### 7.6 多峰

只有次峰同时具备显著性、像素支持、相对主峰幅值、足够分离度，且不是边界或噪声伪影时，才判定为多峰。初始候选条件包括次峰相对主峰达到 `0.2`，并超过背景噪声；检测必须记录 prominence、支持像素、距离、边界和角覆盖。

- 明确次峰但主峰唯一：中心和 D4σ 至少 `caution`；高斯指标为 `unavailable` 或 `caution`；积分指标按自身支持条件判断；
- 两峰竞争主峰：单核心中心和拟合结果 `invalid`；可独立解释的积分诊断按自身条件保留。

### 7.7 高级预处理敏感性

启用滤波、DPC 或坏点修复时，必须同时计算标准分支和高级分支并比较核心指标：

- 任一核心指标相对变化 `>10%`：`caution`；
- 任一核心指标相对变化 `>20%`：不得获得规范 `valid`；
- 无论差异大小，高级分支均必须标记为非默认分析且至少为 `caution`；
- 记录并向用户展示每个核心指标的标准分支值、高级分支值、绝对差、相对差和两侧状态。

### 7.8 原因严重性

原因码严重性顺序固定为：

1. 输入、语义或解码失败；
2. 无可用正信号或背景不可估计；
3. 核心确认饱和、核心严重裁边或主峰不确定；
4. 严重欠采样或关键 ROI 支持不足；
5. 模型不适用、拟合失败或残差过大；
6. provisional 标定、高级预处理、背景降级或轻微中心偏移；
7. 仅非核心诊断不可用。

1–4 使受影响核心指标 `invalid`；5 只否定依赖模型的指标；6 通常为 `caution`；7 不改变独立核心指标状态。报告保留全部原因，不得只保留最高优先级原因。

### 7.9 指标级门控

| 原因 | 中心 | 高斯拟合指标 | D4σ | EE50/80、C(Rref) |
|---|---|---|---|---|
| 无唯一主峰或无正信号 | `invalid` | `invalid` | `invalid` | `invalid` |
| 确认核心饱和 | 仅位置 `caution` | `invalid` | `invalid` | `invalid` |
| 核心半高区裁边 | `caution/invalid` | `invalid` | `invalid` | 按截断程度 `caution/invalid` |
| FWHM `<2 px` | 可用则保留 | `invalid` | `invalid` | 面积支持充分时 `caution` |
| 拟合未收敛、边界命中或残差 `>4σ` | 可用则保留 | `invalid` | 不受影响则照常 | 不受影响则照常 |
| 多峰但主峰唯一 | `caution` | `unavailable/caution` | `caution` | 按自身支持条件 |
| provisional 标定 | 像素域按数据质量判断 | 像素域按数据质量判断 | 像素域按数据质量判断 | 物理域最多 `caution` |
| EE80 未达到 | 可用则保留 | 不受影响 | 不受影响 | EE80 `unavailable` |
| 非标准滤波/DPC/修复 | `caution` | `caution` | `caution` | `caution` |

## 8. 空间标定

- x/y 方向空间标定必须分别记录，包括值、物平面单位、来源、适用范围和确认状态；
- 当前项目可加载 `x=0.42 µm/px`、`y=0.42 µm/px` 的档案，但初始状态必须为 `provisional`；
- 不得把 provisional 档案预填为 confirmed；
- 缺失标定时仍可报告像素域诊断，但不得生成具有物理意义的长度、面积或密度；
- provisional 标定可生成明确带警告的物理域结果，但物理结果不得为 `valid`；
- 非方形像元不得隐式平均；
- 只有用户确认后的标定才可使物理域指标按其他质量条件获得 `valid`；
- 只有成功导出后，本次实际标定才可成为下一次分析的历史初始值。

## 9. 参数权限与版本

### 9.1 用户白名单可调参数

用户只可在契约规定范围内调整：

- 用户确认的 ROI；
- 背景区域；
- x/y 空间标定及确认状态；
- 独立设计参数 `Rref`；
- 显式高级滤波、DPC、坏点修复参数；
- 规定范围内的拟合 ROI/宽度边界。

### 9.2 不可由用户改写

MVP 不允许用户改变：

- 主模型；
- 标准拟合损失；
- 积分离散化规则；
- 坐标和角度约定；
- 背景来源优先级；
- 规范中心算法；
- 质量门槛和状态映射。

任何偏离标准档案的分析必须显式标记为非默认分析、保存完整实际配置并至少降为 `caution`。

每条记录必须保存：分析契约版本、标准参数档案版本、质量档案版本、档案验证状态、算法版本、完整实际参数和输入哈希。修改公式、默认参数、阈值或状态映射必须产生新版本，并把现有工作区结果标记为 stale、要求重新计算；历史记录继续按原版本解释，不得静默重算、覆盖或用新规则重新判定旧报告。

## 10. 人类可读 PDF

人类可读产物固定为可审计 PDF，而不是只包含结果表的摘要。PDF 至少包含：

1. 输入：原始图像渲染预览、资产/记录 ID、哈希、尺寸、位深、通道和编码语义；
2. 标定：x/y 像素尺寸、来源及 `confirmed/provisional` 状态；
3. 处理流程：顺序、是否执行、参数、版本、掩膜和背景统计；
4. 区域与中心：原始/校正视图、背景区、分析 ROI、规范中心和质心诊断；
5. 拟合与掩膜：拟合图像、模型参数、主次轴、角度、残差图、可识别的掩膜视图和收敛状态；
6. 评估：FWHM、D4σ、EE50/80、C(Rref)、质量诊断、每指标状态和原因；
7. 门控：被禁止的指标显示 N/A/null 及原因，不显示被门控的内部原始数值；
8. 复现：契约、参数档案、质量档案、算法版本、实际参数和 record ID。

PDF 可使用适合版面的缩放预览，但不得以 PDF 预览替代机器记录中的数值、元数据和外部资产身份。PDF 与机器记录共享同一个不可变 record ID；重新分析生成新 record ID，不覆盖旧记录。

报告名称由用户指定，可选追加生成时间戳；不得默认使用输入文件名。输入文件始终只读，PDF、JSON、CSV 及相关导出资产必须写入独立输出目录。

## 11. 软件可读历史记录

软件可读历史记录使用稳定结构的 JSON/CSV，保存：

- 全部指标、单位、状态、原因码和 `null` 门控结果；
- 标定、ROI、背景区、预处理、模型、拟合参数、残差和诊断；
- 实际配置及契约/profile/阈值/算法版本；
- 原始图像的资产 ID 或规范化 URI、内容哈希、尺寸、元数据、读取时间和读取状态；
- 用于历史并排视图的校正、拟合、残差和掩膜视图之可重建数据，或这些版本化派生资产的 URI、内容哈希和生成参数；
- 对应 PDF 的 record ID 和生成时间。

历史记录不复制原始图像像素。原始视图在资产可用且哈希匹配时从外部原始图像读取；校正、拟合、残差和掩膜视图必须能由记录数据确定性重建，或通过记录的派生资产引用读取。路径只是定位提示，内容哈希才是资产身份。记录、哈希和引用关系生成后不得静默修改。

外部图像不可访问或哈希不匹配时：

- 指标、状态和配置仍可读取；
- 显示结构化资产错误；
- 禁止图像级对比和重新分析；
- 不得自动寻找同名文件、替换资产或使用当前图像代替。

CSV 必须使用版本化的稳定列名表示值、单位、状态和原因码；JSON 的 `null` 在 CSV 中必须使用明确、稳定且可逆的 null 标记，不得序列化为空字符串。多原因码的编码方式必须随 schema 版本固定并可无损解析。

内部调试数据可以保存被门控的计算尝试值，但必须与普通历史记录和用户导出严格分离，标记为 `non-reportable`，且普通导出代码不得复用这些值。

## 12. 历史对比契约

对比方向固定为：当前记录 `current`，用户选择的历史记录 `reference`。

### 12.1 指标对比

只有以下条件兼容时才计算定量差值：

- 分析契约和 profile 兼容；
- 算法版本兼容；
- 空间标定和单位兼容；
- ROI 语义兼容；
- 预处理和背景语义兼容；
- 模型及指标定义兼容。

对每个兼容数值指标输出：

- current 值；
- reference 值；
- 绝对差 `current-reference`；
- 相对差 `(current-reference)/reference`；
- 单位；
- 两侧状态和原因码；
- 配置差异和状态变化。

reference 为 0、任一值为 `null`、非数值或不兼容时，相对差为 `null`；不兼容时绝对差也为 `null`。软件可以列出不兼容原因，但不得用警告掩盖不可比性，也不得只输出“相似/不同”。

### 12.2 图像并排视图

默认只提供两条记录各自原始、校正、拟合和残差图像的并排视图，并显示各自 ROI、中心和坐标标尺。并排展示不得自动缩放、旋转、平移或配准来修改记录语义。

未来若提供叠加或逐像素差，必须增加显式空间配准步骤并记录变换、误差和算法版本；没有明确配准时，不同尺寸、ROI 或坐标变换的图像不得强行对齐。

## 13. 档案验证前必须冻结的实现细则

本决议已冻结科学语义、标准分支和初始质量门槛，但以下会影响复现性的工程规则必须在 `standard-profile-v1` 或 `quality-profile-v1` 升级为 `validated` 前写成唯一、可执行、版本化的定义，并接受同一回归套件验证；它们不得留给不同实现自行选择，也不得由用户临时调整：

- 背景帧“采集条件匹配”的逐字段接受条件；
- 稳健一阶仿射背景拟合的具体估计器、坐标归一化、停止条件及残差判定；
- 背景区中“核心候选”和“明显正信号候选”的排除阈值与连通规则；
- SNR 中 `peak`、空间变化 `background`、MAD 采样区域、掩膜排除和峰值提取的操作定义；
- `P_peak` 的取值邻域、`core_mask` 的连通性、亚像素中心归属、并列峰处理，以及核心有效像素比例的分母；
- 高斯宽度初始化在“局部半高”和“ROI 尺寸”之间的唯一选择规则，以及宽度上限公式；
- RMS 和 L1 残差使用的有效掩膜、像素/面积权重及量纲；
- 多峰检测的 prominence、最低支持像素、最小分离度和边界排除数值。

上述细则未全部冻结并通过验收时，profile 必须保持 `provisional`，因此结果仍受“最多 `caution`”的全局上限约束。

## 14. 回归验收

`standard-profile-v1` 和 `quality-profile-v1` 必须使用 Issue #4 的真实和合成代表性输入进行版本化验收。每个合成样例记录随机种子、模型、生成参数、位深、背景、噪声、clip、输入哈希和预期状态。

### 14.1 数值容差

圆高斯和旋转椭圆高斯：

- 主/次轴 FWHM 相对解析值误差 `≤5%`；
- D4σ 相对解析值误差 `≤5%`；
- EE50/EE80 半径绝对误差 `≤0.02·FWHM`；
- 椭圆主轴角误差 `≤2°`，按 180° 周期比较；
- 中心误差 `≤0.5 px`。

理想高斯内部一致性：

- `D4σ/FWHM` 理论值 `1.698643…`，验收区间 `[1.65, 1.75]`；
- `EE50/FWHM` 验收区间 `[0.48, 0.52]`；
- `EE80/FWHM` 验收区间 `[0.742, 0.782]`。

这些区间是算法和离散化验收容差，不是实验测量不确定度，不得因单次测试失败而临时放宽。

### 14.2 场景状态基线

以下 `valid` 均指 profile 已升级为 `validated` 后的预期；profile 尚为 `provisional` 时，对应结果按全局规则封顶为 `caution`。

- 干净圆高斯：解析指标在容差内，预期 `valid`；
- 干净旋转椭圆高斯：尺寸和角度在容差内，预期 `valid`；
- 背景梯度：背景校正和残差通过时 `valid`，否则 `caution/invalid`；
- 双高斯：单核心高斯指标 `unavailable/caution`，独立积分诊断按自身条件保留；
- Airy/环结构：高斯模型指标 `unavailable/caution`，不得把旁瓣当主峰；
- 低 SNR：按本规范 SNR 阈值为 `invalid`；
- clip/确认核心饱和：受影响定量指标 `invalid` 且数值为 `null`；
- 坏点：核心和积分支持充分时 `caution`，不足时 `invalid`；
- 裁边：受影响的 EE、D4σ、FWHM 按门控表降级或无效；
- 真实 8-bit PNG：编码语义或标定未确认时，不得成为 `valid` 的物理测量基线。

## 15. 后续实现验收清单

- [ ] 只接受单张图像、单个用户确认矩形 ROI、单次分析；其他范围显式拒绝。
- [ ] 实现固定预处理顺序并保留有符号浮点校正强度图与全部掩膜。
- [ ] 输入编码语义不明和彩色 RGB 按本规范确认或拒绝，不发生静默转换。
- [ ] 坏点先于背景统计被掩膜；默认不修复、不滤波、不 DPC。
- [ ] 背景帧优先，保护区仿射模型满足最低支持；不使用静默全图中值兜底。
- [ ] 规范中心只在独立定位副本上计算，测量数组不被平滑覆盖。
- [ ] 所有规范指标使用同一确认 ROI、规定中心、信号和离散化定义。
- [ ] 旋转椭圆高斯模型、初始化、边界、目标函数和输出字段符合契约。
- [ ] 每个指标独立产生状态、全部原因码和报告门控后的值。
- [ ] `invalid/unavailable` 对外值为 `null`，确认核心饱和等严重原因不泄露内部数值。
- [ ] x/y 标定独立记录；0.42/0.42 µm/px 档案保持 provisional，直到用户确认。
- [ ] 用户只能调整白名单参数；任何非标准分支显式版本化并执行敏感性比较。
- [ ] PDF 包含原始、校正、拟合、残差和掩膜的可审计过程，并与机器记录共享 record ID。
- [ ] 报告使用用户指定名称和独立输出目录，输入文件保持只读。
- [ ] JSON/CSV 保存完整结果、状态、配置、版本和外部图像资产身份，但不复制原始像素。
- [ ] CSV 使用稳定 schema 和显式 null 标记，不把 null 写为空字符串。
- [ ] 历史记录可确定性重建或引用校正、拟合、残差和掩膜视图。
- [ ] 历史对比先检查兼容性，再计算 current-reference 绝对差和相对差。
- [ ] 图像对比默认为并排展示，不自动配准；资产缺失时指标仍可读，但图像对比和重分析被禁止。
- [ ] Issue #4 的全部真实/合成场景和数值容差形成自动回归测试。
- [ ] profile 未完成全部验收前保持 provisional，任何结果最多为 `caution`。
- [ ] 规则变更生成新契约/profile 版本，不覆盖或重新解释历史记录。

## 16. 非目标

本 Issue 不实现分析代码、UI、报告生成器或历史对比功能；它只冻结这些后续实现必须遵循的契约。以下内容不属于 MVP 默认主指标或承诺：

- 不同设备或批次之间未经校准证明的可比性；
- 任意彩色图像的自动灰度化；
- 自动图像配准或逐像素比较；
- 旁瓣/环和 overlap 作为默认质量主指标；
- 用测得 FWHM 反推 Rref；
- 把固定口径集中度称为 Strehl；
- 通过修改质量阈值把失败分析改判为 valid。
