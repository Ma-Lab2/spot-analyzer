---
issue: 9
status: accepted
contract: validation-contract-v1
date: 2026-09-09
profile_validation: provisional
---

# 算法验证方法与验收容差

> **验证范围扩展通知（2026-09-13）**：Issue #75 将严格等通道的 8 位 RGB PNG、自动主焦斑/ROI/背景候选、预览/正式结果和多图片工作区纳入后继验证范围。本文件关于科学计算域、质量状态、解析 oracle、数值容差、原因码、确定性和真实图片不充当绝对物理真值的原则继续有效；其中排除全部 RGB 样例及只描述逐项确认 ROI 的产品流程不再代表后继客户端验收范围。

## 1. 决议范围

本决议冻结规范焦斑分析流程的验证证据、代表性场景、解析基准、数值容差、质量状态断言、可复现性规则和 profile 升级门槛。

本决议服务于：

- `analysis-contract-v1`；
- `standard-profile-v1`；
- `quality-profile-v1`；
- Issue #4 的真实与合成代表性输入；
- Issue #6 的分析契约与测量有效性规则；
- Issue #7 的 Python 分析核心和 worker 边界；
- Issue #8 的 `AnalysisRecord` 和导出契约。

本决议不实现分析核心、UI、报告生成器或完整回归 harness。它规定后续实现必须怎样被验证，以及什么结果才可称为通过。

验证完成前，两个 profile 的 `profile_validation` 必须保持 `provisional`。通过 Issue #10 的完整回归并经 Issue #11 审查接受后，才可以将 profile 状态升级为 `validated`。

## 2. 已确认的验证原则

### 2.1 四层证据

验证分为四层：

1. **解析合成验证**：使用无噪声、无量化、无裁边的圆高斯和旋转椭圆高斯，比较解析 oracle 与测量结果。
2. **扰动合成验证**：加入背景梯度、噪声、坏点、饱和、裁边、多峰和旁瓣，验证诊断、原因码、掩膜、状态和可追溯性。
3. **真实 PNG 回归**：验证解码、哈希、输入语义、重复性和质量警告。真实 PNG 缺少足够的物理真值时，不作为绝对数值金标准。
4. **接口一致性验证**：验证核心直接调用、PNG 输入适配器、NDJSON worker、报告和未来调用者之间不改变分析记录语义。

计算成功、拟合收敛或产生数值不等于测量有效。所有测试必须分别断言流程状态、指标状态、对外值和原因码。

### 2.2 标准分支与高级分支

标准分支固定为：

- 输入图像原样解码；
- 记录编码语义，不静默 gamma、色彩或位深转换；
- 暗场/平场仅在条件匹配且用户提供时执行；
- 坏点标记并掩膜，默认不修复；
- 匹配背景帧优先，否则使用独立保护区上的稳健一阶仿射背景；
- 保留有符号浮点校正强度图 `S`；
- 能量积分使用 `P = max(S, 0)`；
- 独立定位副本使用固定平滑峰中心算法；
- 所有指标使用同一用户确认矩形 ROI；
- 使用旋转椭圆高斯有界普通最小二乘基线。

滤波、DPC、坏点插值、稳健拟合或其他偏离标准档案的处理只能作为显式高级分支。高级分支必须与标准分支同时计算并比较，不能替代标准结果，也不能因为更接近 oracle 而自动获得规范 `valid`。

## 3. 代表性验证矩阵

### 3.1 合成场景

每个场景生成：

- 未量化浮点参考 `校正强度图`；
- 实际测试输入图像；
- 场景参数 manifest；
- 参考数组和编码输入哈希；
- 预期质量状态、原因码和适用容差。

默认网格和坐标：

- 尺寸 `256 × 256`；
- 像元间距 `0.1 µm/px`；
- 默认中心 `(127.5, 127.5)`；
- 除非场景明确测试亚像素中心，不改变上述约定。

必需场景：

| ID | 场景 | 主要验收对象 |
|---|---|---|
| `gaussian_circular` | 无噪声圆高斯 | 中心、FWHM、D4σ、EE50/EE80、内部关系 |
| `gaussian_elliptical_rotated` | 旋转椭圆高斯 | 主次轴、角度、轴排序、物理坐标变换 |
| `two_gaussian_multimodal` | 已知双峰 | 多峰诊断、中心歧义、单高斯失配状态 |
| `airy_sidelobe` | Bessel/Airy 旁瓣 | 环/旁瓣候选、非高斯残差，不误报为主峰 |
| `background_gradient` | 高斯加一阶背景梯度 | 背景恢复、残差、过扣/欠扣敏感性 |
| `hot_dead_pixels` | 已知亮点和暗点 | 坏点 mask、标准/高级分支差异 |
| `low_snr_seeded` | 固定种子的低 SNR 高斯 | SNR 门控、重复性和确定性 |
| `saturated_core` | 核心连续 clip 到最大码值 | 饱和诊断、受影响指标 `invalid/null` |
| `cropped_edge` | 一侧穿过窗口边界的高斯 | `window_truncated`、指标降级和下限语义 |

### 3.2 真实 PNG

真实样例只从 Issue #4 manifest 指向的外部只读目录读取，并通过 SHA-256 验证身份。至少覆盖：

- 空白帧：`20251016/2.png`；
- 弱单峰/低对比：`20251016/1.png`、`20251016/3.png`；
- 同批次重复/对照：`20251016/*`；
- 最大码值候选：`20251016/1.png`、`20251016/3.png`、`20260607/*`；
- 疑似孤立亮点：`20260607/1-7.png`、`20260607/2-7右下.png`；
- 背景纹理/边界候选：`20260607/2-7右下.png`、`20260607/2-8（1）.png`。

RGB 样例和显示导出物不作为当前灰度测量回归输入。`max=255` 只能触发 `possible_saturation`，不能单独证明物理饱和。

## 4. 解析 oracle 与数值容差

### 4.1 圆高斯

使用：

```text
I(r) = A exp(-2r² / w²) + B
```

解析关系：

```text
FWHM = w * sqrt(2 ln 2)
D4σ = 2w
EE(R) = 1 - exp(-2R² / w²)
EE(FWHM / 2) = 0.5
```

### 4.2 旋转椭圆高斯

使用：

```text
I = A exp[-2(u² / wx² + v² / wy²)] + B
```

其中 `u/v` 是按已知角度旋转后的坐标。解析关系：

```text
FWHM_major = wx * sqrt(2 ln 2)
FWHM_minor = wy * sqrt(2 ln 2)
D4σ_major = 2wx
D4σ_minor = 2wy
```

圆形 EE 使用同一离散网格上的数值 oracle，不把圆高斯关系错误套用到椭圆主轴。

### 4.3 必需数值容差

对无噪声、无量化、无裁边的圆高斯和旋转椭圆高斯：

- 主/次轴 FWHM 相对解析值误差 `≤5%`；
- D4σ 相对解析值误差 `≤5%`；
- EE50/EE80 半径绝对误差 `≤0.02 × FWHM`；
- 椭圆主轴角误差 `≤2°`，按 180° 周期比较；
- 规范中心误差 `≤0.5 px`。

理想圆高斯内部一致性：

- `D4σ/FWHM` 在 `[1.65, 1.75]`；理论值为 `1.698643…`；
- `EE50/FWHM` 在 `[0.48, 0.52]`；
- `EE80/FWHM` 在 `[0.742, 0.782]`。

这些是算法和离散化验收容差，不是实验测量不确定度。单次测试失败不得临时放宽。

比较规则：

- 所有 `≤` 都包含边界；
- 相对误差为 `abs(measured-oracle)/abs(oracle)`；
- oracle 为零时使用 manifest 中明确的绝对容差；
- EE 半径容差使用对应解析场景的参考 FWHM；
- 角度误差使用 180° 周期距离；
- 像素域与物理域分别比较；物理域还必须通过标定状态门控。

## 5. 随机性与重复性

`low_snr_seeded` 使用固定种子 `0–31`，共 32 个种子。每个种子必须：

- 在相同生成器版本下产生相同参考数组和输入哈希；
- 重复运行产生相同状态、原因码和诊断；
- 不发生未捕获异常或协议崩溃；
- 按质量 profile 达到预期的 `invalid`/`caution` 状态。

整组结果报告：

- 偏差中位数；
- MAD；
- 95 分位绝对偏差；
- 状态和原因码分布。

这些聚合数值用于诊断，不作为低 SNR 接近无噪声 oracle 的额外放行条件，因为 `SNR <5` 的场景按契约本身应被质量门控。

## 6. 可执行的工程规则

### 6.1 匹配背景帧

背景帧只有在以下条件全部满足时才自动视为匹配：

- 尺寸、通道、位深和 ROI 一致；
- 曝光、增益、温度、光路/焦距一致；
- 来自同一明确采集批次；
- 所需元数据均存在。

缺少必要字段时不得自动判定匹配。用户可以显式选择非匹配背景降级，但必须记录 `background_match_unverified`，相关指标最多为 `caution`。合成正例使用完全一致元数据，负例一次只改变一个匹配字段。

### 6.2 背景估计和保护区排除

无匹配背景帧时：

1. 在独立保护区上做临时稳健位置和尺度估计；
2. 排除坏点、饱和点、越界点以及满足以下任一条件的像素：
   - `S - B > 3 × sigma_background`；
   - `S - B > 0.10 × P_peak`；
3. 对排除的 8 连通区域膨胀 1 px；
4. 对剩余像素使用 Huber IRLS 拟合一阶仿射模型；
5. x/y 归一化到 `[-1, 1]`；
6. 最大迭代次数 50；
7. 参数变化和目标函数变化均小于 `1e-8` 时收敛。

必须记录临时阈值、排除 mask、有效像素数、排除像素数、x/y 覆盖、背景残差和收敛状态。禁止静默降级为全图中值。

背景支持要求：

- 有效背景像素数至少为模型参数数量的 10 倍；
- x 和 y 方向均至少跨越 3 个不同像素行/列；
- 支持不足时，依赖背景的指标为 `invalid`。

背景残差使用保护区残差的稳健标准化尺度：

```text
background_residual_scale = 1.4826 * MAD(background_residuals)
```

保护区残差 RMS 相对该尺度：

- `≤1.5`：通过；
- `>1.5 且 ≤2.5`：背景降级，相关指标 `caution`；
- `>2.5`：背景不可信，相关指标 `invalid`。

### 6.3 SNR

```text
SNR = (peak - background) / sigma_background
sigma_background = 1.4826 * MAD
```

具体操作：

- `peak` 使用规范中心 `3×3` 邻域内最大有效 `S` 值；
- `background` 使用仿射背景在规范中心处的预测值；
- MAD 在独立保护区的背景残差上计算；
- 排除坏点、饱和点、核心候选、明显正信号候选和越界点；
- 有效样本数至少为 `max(30, 10 × 背景模型参数数目)`。

状态门槛：

- `SNR ≥10`：通过；
- `5≤SNR<10`：`caution`；
- `SNR<5` 或背景噪声不可估计：相关定量指标 `invalid`。

MAD 为零且残差全部为零时，只有峰值也为零才可视为无噪声；否则使用 `background_noise_unavailable`，不得给出 `valid`。

### 6.4 核心掩膜

```text
core_mask = P >= 0.5 * P_peak
```

规则：

- `P_peak` 为规范中心四舍五入像素周围 `3×3` 邻域内最大有效正信号；
- 使用 8 邻域连通；
- 亚像素中心归属最近像素，平局按 `(y,x)` 字典序；
- 分母为阈值连通区域全部像素；
- 分子为其中未坏点、未饱和、未越界且参与测量的像素；
- 坏点和饱和点保留在分母中；
- 接触 ROI 边界时产生 `core_clipped`。

有效比例门槛：

- `≥0.95`：通过；
- `0.8–<0.95`：`caution`；
- `<0.8`：核心尺寸指标 `invalid`。

### 6.5 高斯初始化和边界

- 优先使用 x/y 方向局部半高宽；
- 任一方向半高交点缺失、饱和、触边或不属于同一连通半峰区间时，统一回退；
- 回退初值：`sigma = min(roi_width, roi_height) / 6`；
- `sigma` 下限：`0.5 px`；
- `sigma` 上限：`min(roi_width, roi_height) / 2`；
- 回退、半高不可用和上限命中均记录原因码；
- 不因优化器成功而动态放宽边界。

### 6.6 残差

拟合和残差使用同一个有效域，排除：

- 坏点；
- 饱和点；
- 越界点；
- 无法解码点；
- 明确无效背景点。

标准分支使用等权有界普通最小二乘。残差定义为 signed：

```text
R = S - G
RMS_residual = sqrt(mean(R²))
L1/T = sum(abs(R) * delta_A) / sum(P * delta_A)
```

必须记录有效像素数、面积权重、mask 哈希和残差单位。不得用 `P` 替代 signed residual，也不得把高级加权拟合的结果当作标准结果。

### 6.7 多峰

次峰候选必须同时满足：

- prominence `≥0.20 × P_peak`；
- prominence `≥5 × sigma_background`；
- 8 连通支持像素不少于 9 个；
- 与主峰中心距离至少 `max(3 px, 0.5 × FWHM_major)`；
- 候选连通区不接触 ROI 边界。

记录候选位置、prominence、支持像素、分离度和边界距离。环结构单独产生 `ring_candidate`，不直接计为双峰。

- 次峰明确但主峰唯一：中心和 D4σ 至少 `caution`；高斯指标为 `unavailable` 或 `caution`；
- 两峰竞争主峰：单核心中心和高斯指标 `invalid`；独立积分诊断按自身条件判断。

## 7. 质量状态断言

### 7.1 场景状态基线

| 场景 | 原始质量预期 | 说明 |
|---|---|---|
| 干净圆高斯 | `valid` | 数值 oracle 和支持条件均通过 |
| 干净旋转椭圆高斯 | `valid` | 尺寸、角度和中心通过 |
| 背景梯度 | `valid` 或 `caution/invalid` | 取决于背景模型和残差 |
| 双高斯 | `unavailable/caution` | 单高斯不代表整幅真模型 |
| Airy/环 | `unavailable/caution` | 不把旁瓣当主峰 |
| 低 SNR | `invalid` | `SNR<5` 时相关指标无效 |
| 确认核心饱和 | `invalid` | 受影响数值对外为 `null` |
| 坏点 | `caution` 或 `invalid` | 由有效支持决定 |
| 裁边 | 降级或 `unavailable` | 受影响指标带 `window_truncated` |
| 空白真实帧 | `unavailable` 或整次 `invalid` | 不得以零值伪装理想焦斑 |

### 7.2 profile 封顶

测试同时保存并断言：

- `quality_status_before_profile_cap`；
- `reported_status`。

在 `provisional` profile 下，原始 `valid` 对外封顶为 `caution`，并增加 `provisional_profile`。`invalid` 和 `unavailable` 不得因 profile 升级而提升。

### 7.3 真实 PNG 原因码

真实 fixture 使用“必需集合 + 允许扩展”规则：

- 必需原因码必须全部出现；
- 禁止原因码不得出现；
- 允许非冲突的新增诊断码；
- 原因码顺序必须稳定；
- 不要求输出没有物理真值的指标数值。

例如最大码值样例可要求 `possible_saturation`，但在无独立核心平顶证据时禁止 `saturated_core`。RGB 样例不得静默转灰度。

## 8. 可复现性

### 8.1 固定环境

锁定 Python、NumPy、SciPy、Pillow、算法版本、依赖清单和构建 manifest 的官方环境必须得到：

- 相同测量数值；
- 相同指标状态；
- 相同原因码；
- 相同 mask 统计；
- 相同非时间派生资产内容。

`record_id`、生成时间、读取时间和输出路径可以不同。

### 8.2 跨环境

其他环境只承诺语义一致：

- 状态、原因码、mask 计数、场景分类和门控结果一致；
- 数值满足：

```text
abs(a-b) <= 1e-12 + 1e-10 * abs(reference)
```

- 角度使用 180° 周期距离；
- canonical JSON、数组排序、原因码和诊断键顺序稳定；
- 不用报告显示小数位隐藏核心差异。

## 9. 回归 manifest 和资产策略

每个合成场景 manifest 至少保存：

- 场景 ID；
- 生成器版本；
- 随机种子；
- 数组尺寸和像元间距；
- 中心；
- 解析模型和全部参数；
- 位深、背景、噪声和 clip 规则；
- 参考数组哈希；
- 编码输入哈希；
- `quality_status_before_profile_cap`；
- `reported_status_by_profile_validation`；
- 必需/禁止原因码；
- 指标容差、单位和比较方式；
- 契约、profile 和算法版本。

Git 中提交生成器、manifest、oracle 定义和少量小型 fixture。大型参考数组和 PNG 在测试时生成并校验哈希，CI 保存生成结果 artifact。生成器不得依赖当前时间、机器路径、未锁定随机源或显示渲染。

真实 PNG 只保存外部路径提示和 SHA-256，不复制原始数据。输入路径存在但哈希不一致时测试失败。

## 10. 外部 fixture 不可用时

普通开发运行：

- 标记 `fixture_unavailable`；
- 允许合成测试继续。

发布或 profile 验证：

- 真实 PNG 是必需回归；
- 缺失时验证状态为 `incomplete`，不得宣称通过；
- fixture 缺失不等于算法失败，但也不等于通过；
- 禁止自动寻找同名替代文件或使用当前图像替代。

## 11. 验证层次和后续实现

Issue #10 应按以下层次实现和运行：

1. 核心直接调用：内存 `InputImage` 与 `analyze`；
2. 输入适配器：PNG 解码、8/16 位、端序、RGB 通道规则；
3. worker 协议：`analysis-request-v1`、`analysis-event-v1`、`analysis-result-v1`；
4. 报告契约：`record_id`、门控后的 `null/N/A`、输入只读和同名不覆盖；
5. UI/CLI 一致性：相同配置快照得到相同 `AnalysisRecord`，显示设置不改变测量结果。

标准分支必须通过全部数值、状态、原因码、确定性和接口契约测试。高级分支的敏感性比较、参数记录和降级规则也属于质量契约；其失败阻塞 `quality-profile-v1` 验证，但不能替换标准分支。

## 12. Profile 升级门槛

`standard-profile-v1` 和 `quality-profile-v1` 只有在以下条件全部满足后才可从 `provisional` 升级为 `validated`：

- 所有必需合成场景完成；
- 所有低 SNR 固定种子完成；
- 所有必需真实 PNG fixture 可访问且哈希匹配；
- 解析数值容差全部通过；
- 状态、原因码、mask、背景统计和可追溯性全部通过；
- 标准/高级预处理敏感性规则通过；
- 固定环境重复运行一致；
- worker 和报告契约不改变记录语义；
- 没有未记录的失败、跳过或 fixture 缺失；
- Issue #10 记录完整结果；
- Issue #11 完成正式规格审查并接受。

数值通过但操作定义、原因码、记录字段或确定性不符合本决议，不能视为通过。修改公式、阈值、默认参数、状态映射或操作定义必须生成新的 profile/算法版本，并重新运行完整回归。

## 13. 交付边界

### 已决策

- 四层验证证据；
- 合成场景和真实 PNG 回归矩阵；
- 解析 oracle 和数值容差；
- 32 个低 SNR 固定种子；
- 背景、SNR、核心 mask、拟合初始化、残差和多峰规则；
- profile 封顶和升级门槛；
- 固定环境与跨环境确定性；
- manifest、fixture 和哈希策略。

### 待实现

- 合成生成器；
- manifest loader 和 oracle 比较器；
- `analyze` 核心测试；
- PNG 输入回归；
- worker NDJSON 测试；
- 报告契约测试；
- UI/CLI 一致性测试。

### 待验证

- 当前环境中外部真实 PNG 的可访问性和哈希；
- 全部合成场景的实际输出；
- 干净 Windows 环境下的完整 worker/报告烟测；
- `standard-profile-v1` 和 `quality-profile-v1` 是否满足升级条件。

Issue #9 不把尚未运行的测试或尚未实现的能力称为已完成，不在本票中把 profile 标记为 `validated`。
